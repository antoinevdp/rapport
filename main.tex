\documentclass{article}

% Language setting
\usepackage[T1]{fontenc}
\usepackage[french]{babel}

% Set page size and margins
\usepackage[includeheadfoot,a4paper,top=0.5cm,bottom=2cm,left=2.5cm,right=2.5cm,marginparwidth=1.75cm]{geometry}

% Useful packages
\usepackage{amsmath}
\usepackage{graphicx}
\usepackage{titlesec}
\usepackage[colorlinks=true, allcolors=blue]{hyperref}
\usepackage{fancyhdr}
\pagestyle{fancy}
\usepackage{lastpage}
\renewcommand\headrulewidth{1pt}

\usepackage{listings}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric, calc, shadows.blur, fit}

\usepackage{array}
\usepackage{mdframed}
\usepackage{makecell}
\usepackage{seqsplit}
\usepackage{tabularx}
\usepackage{colortbl}
\usepackage{enumitem}
\usepackage{needspace}
\usepackage{float}
\usepackage{caption}

\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.95,0.95,0.92}

\lstdefinestyle{mystyle}{
    backgroundcolor=\color{backcolour},   
    commentstyle=\color{codegreen},
    keywordstyle=\color{magenta},
    numberstyle=\tiny\color{codegray},
    stringstyle=\color{codepurple},
    basicstyle=\ttfamily\footnotesize,
    breakatwhitespace=false,         
    breaklines=true,                 
    captionpos=b,                    
    keepspaces=true,                 
    numbers=left,                    
    numbersep=5pt,                  
    showspaces=false,                
    showstringspaces=false,
    showtabs=false,                  
    tabsize=2
}

% ===================== COULEURS =============================
\definecolor{titleNavy}{RGB}{22,50,92}
\definecolor{accentBlue}{RGB}{30,110,180}
\definecolor{tableHeader}{RGB}{22,50,92}
\definecolor{tableRowAlt}{RGB}{238,242,248}
\definecolor{ruleGray}{RGB}{200,205,212}
\definecolor{validGreen}{RGB}{36,130,70}
\definecolor{bronzeColor}{RGB}{180,110,60}
\definecolor{bronzeBg}{RGB}{255,240,220}
\definecolor{silverColor}{RGB}{90,100,115}
\definecolor{silverBg}{RGB}{230,235,242}
\definecolor{goldColor}{RGB}{160,130,0}
\definecolor{goldBg}{RGB}{255,252,210}
\definecolor{cloudColor}{RGB}{30,110,180}
\definecolor{cloudBg}{RGB}{220,238,255}
\definecolor{mlColor}{RGB}{80,140,60}
\definecolor{mlBg}{RGB}{225,245,220}

% ---- Figures encadrées avec légende (captures du projet) ----
\captionsetup{font={small,it}, labelfont={bf,color=titleNavy}, labelsep=endash, justification=centering}
\newcommand{\figc}[3]{% #1 = fichier (dans figures/), #2 = largeur, #3 = légende
  \begin{figure}[H]
    \centering
    \setlength{\fboxsep}{0pt}%
    \fcolorbox{ruleGray}{white}{\includegraphics[width=#2]{figures/#1}}%
    \caption{#3}
  \end{figure}}


% ===================== STYLES TIKZ ==========================
\tikzset{
  medalbox/.style={
    rectangle, rounded corners=5pt, very thick,
    text width=11.5cm, minimum height=1.5cm, align=center,
    font=\small, inner sep=8pt
  },
  flowbox/.style={
    rectangle, rounded corners=4pt, thick,
    text width=9cm, minimum height=1.2cm, align=center,
    font=\small, inner sep=6pt
  },
  minibox/.style={
    rectangle, rounded corners=4pt, thick,
    text width=5.2cm, minimum height=1.3cm, align=center,
    font=\small, inner sep=6pt
  },
  arrowM/.style={-{Stealth[scale=1.3]}, line width=1.4pt, draw=black!50},
}
\newcommand{\fitwidth}[1]{\resizebox{\ifdim\width>\linewidth\linewidth\else\width\fi}{!}{#1}}
\newcommand{\code}[1]{\texttt{\seqsplit{#1}}}

\mdfdefinestyle{captureStyle}{
  linecolor=black!30, linewidth=0.8pt, roundcorner=5pt,
  backgroundcolor=black!4,
  innertopmargin=7pt, innerbottommargin=7pt,
  innerleftmargin=12pt, innerrightmargin=12pt,
  skipabove=8pt, skipbelow=8pt, nobreak=true,
}
\newcommand{\capture}[1]{%
  \begin{mdframed}[style=captureStyle]
    \centering\small\textit{\textbf{\textcolor{black!60}{[Capture]}}\quad #1}
  \end{mdframed}%
}


\lstset{style=mystyle}

\pagestyle{fancy}
\fancyhf{}

\fancyhead[C]{\Large Efrei\\ \Large Rapport de projet}
\fancyhead[R]{\includegraphics[width=2cm]{assets/logo-efrei.png}}

\fancyfoot[C]{Version 2.0 Page \thepage/\pageref{LastPage}}
\setlength{\headheight}{2cm}

\fancypagestyle{plain}{
    \fancyhf{}
    \fancyhead[C]{\Large Efrei\\ \Large Rapport de projet}
    \fancyhead[R]{\includegraphics[width=2cm]{assets/logo-efrei.png}}
    \fancyfoot[C]{Version 2.0 Page \thepage/\pageref{LastPage}}
    \renewcommand\headrulewidth{1pt}
}


\title{Rapport de Projet Certifiant}
\author{Tonio}
\date{\today}

\begin{document}

\hypersetup{pageanchor=false}
\begin{titlepage}
    \thispagestyle{empty}
    \centering

    \vspace*{1.5cm}
    \includegraphics[width=4cm]{assets/logo-efrei.png}\\[1.5cm]

    {\Large EFREI Paris\\[0.3cm]}
    {\large Master 2 Data Engineering \& IA\\[0.5cm]}
    {\large RNCP36739 -- Expert en Ingénierie de données\\[2cm]}

    {\Huge \bfseries Rapport de validations des blocs de compétences\\[0.8cm]}

    \begin{tabular}{rl}
        Auteur :     & Tremont-Raimi Julien \& Vandeplanque Antoine \\
        Promotion :  & 2024--2026                                   \\
        Encadrant :  & A completer                                  \\
        Entreprise : & A completer                                  \\
    \end{tabular}

    \vfill

    {\large \today}
\end{titlepage}

\hypersetup{pageanchor=true}
\pagenumbering{roman}
\tableofcontents
\clearpage

\pagenumbering{arabic}

\section*{Remerciements}
\addcontentsline{toc}{section}{Remerciements}

Je tiens à exprimer mes remerciements les plus sincères au intervenants et professeurs.
La richesse de leurs enseignements ont grandement enrichi mes connaissances
et affûté mes compétences ; leur encadrement, leur conseils, prodigués tout au long de
cette formation, ont été  essentiels de ma progression.

Enfin, je voudrais exprimer ma profonde gratitude envers toutes les personnes de Geismar pour le
cadre d'apprentissage qu'elles m'ont offert. Cet environnement a joué un rôle déterminant dans mon
développement professionnel en me permettant d'acquérir de nouvelles compétences. Je tiens à souligner
combien les opportunités d'expérimentation qui m'ont été offertes ont été d'une valeur inestimable.

\medskip
\noindent À tous, merci pour votre contribution essentielle à la concrétisation de ce
projet.

\newpage
\section{Introduction}

La donnée s'est imposée comme l'un des actifs stratégiques majeurs des organisations
contemporaines. Derrière chaque tableau de bord, chaque modèle prédictif et chaque
service numérique se trouve une chaîne d'ingénierie qui transforme des flux bruts,
massifs et hétérogènes en information fiable, exploitable et créatrice de valeur.
C'est précisément ce métier de \textbf{data engineer} que ce rapport entend illustrer
: concevoir, construire et industrialiser cette chaîne, de la source jusqu'à la décision.

Ce document constitue le dossier écrit de certification professionnelle \textbf{RNCP36739}
\og{} Expert en Ingénierie de Données \fg{}, produit dans le cadre du Mastère Data Engineering \& IA
d'EFREI Paris (promotion 2024--2026). Il vise à démontrer l'acquisition des cinq blocs de compétences
du référentiel~\cite{rncp} à travers des \textbf{réalisations concrètes} plutôt que des connaissances
théoriques isolées.

La démonstration s'appuie sur cinq projets complémentaires, chacun choisi pour
illustrer au mieux un pan du métier : \textbf{Jobtech}, plateforme d'agrégation de
données d'emploi tech à partir de six sources hétérogènes ; Le \textbf{project cloud},
plateforme serverless AWS de mobilité urbaine en temps réel ; Le \textbf{projet spark},
pipeline Big Data distribué sur données aéronautiques ; \textbf{fizzbuzz Efrei},
chaîne d'intégration et de déploiement continus conteneurisée ; et le
\textbf{projet datas cience project}, système de maintenance prédictive industrielle par
apprentissage automatique. Le tableau ci-dessous récapitule leur rattachement aux
blocs du référentiel.

\needspace{12\baselineskip}
\begin{center}
\begin{tabularx}{\linewidth}{|l|>{\raggedright\arraybackslash}X|l|}
\hline
\rowcolor{tableHeader}
\thead{Projet} & \thead{Domaine} & \thead{Blocs couverts} \\
\hline
Jobtech & Agrégation de données d'emploi tech européen & Bloc 1 (principal), Bloc 5 \\
\hline
project\_cloud & Plateforme temps réel de mobilité (AWS) & Bloc 1 (NoSQL), Bloc 3, Bloc 5 \\
\hline
projet\_spark & Pipeline Big Data aéronautique US & Bloc 2 (principal) \\
\hline
fizzbuzz-Efrei & Chaîne CI/CD et conteneurisation & Bloc 2 (DevOps) \\
\hline
data\_science\_project & Maintenance prédictive industrielle (ML) & Bloc 4 (principal) \\
\hline
\end{tabularx}
\end{center}

Chaque bloc suit une structure identique --- compétences visées, contexte et enjeux,
architecture, réalisations techniques commentées, axes d'amélioration et synthèse
reliée au référentiel --- de sorte que chaque partie soit autonome et directement
évaluable. Le rapport est précédé d'une présentation de l'entreprise d'alternance,
puis d'une synthèse de la couverture des cinq blocs de compétences.

\section{Présentation d'entreprise}

\subsection{L'entreprise : Geismar}

Fondée en 1924 à Colmar par Lucien Geismar, la société conçoit, fabrique et vend
dans le monde entier des outils et des machines destinés à la pose et à l'entretien
des voies ferrées et des caténaires.

Le siège social est aujourd'hui situé à la Défense. L'entreprise compte
750~salariés, dont 420 en France, et possède cinq usines : trois en France (Colmar,
Marseille et Saint-Didier-de-la-Tour) et deux à l'étranger (Italie et États-Unis).

Geismar est présent commercialement dans de nombreux pays, notamment en France, au
Royaume-Uni, aux États-Unis, au Brésil, en Allemagne, en Italie, en Espagne, au
Canada et en Afrique du Sud. Le groupe réalise environ 85\,\% de son chiffre
d'affaires à l'étranger.

\subsection{Le poste : Alternant Data Engineer}

\subsubsection{Contexte}

Geismar utilise différents systèmes ERP selon les filiales : M3, Business Central,
ou encore des bases Access héritées de systèmes plus anciens. Cette diversité crée un
besoin de centralisation et d'harmonisation des données à l'échelle du groupe.

\subsubsection{Missions}

Mon rôle consiste à développer des pipelines de données en Python pour extraire,
transformer et charger les données issues des différents ERP. Ces données sont
ensuite structurées via une solution de big data selon une architecture
Bronze-Silver-Gold, qui permet de passer progressivement des données brutes à des
données exploitables pour le reporting.

Je travaille principalement sur les données financières, la gestion des stocks, le
suivi des commandes et le reporting opérationnel des différentes filiales. Une partie
importante de mon travail consiste également à migrer les données vers Snowflake.

\subsubsection{Environnement technique}

J'utilise principalement Python et SQL pour le développement. Les données sont
stockées sur SQL Server et Snowflake. L'infrastructure cloud repose sur Microsoft
Azure, notamment Blob Storage pour le stockage et Azure DevOps pour le versionning et
les pipelines. Côté restitution, les données alimentent des rapports Power BI.

\section{Synthèse des blocs de compétences RNCP36739}

La certification \og{}Expert en Ingénierie de Données\fg{} s'articule autour de cinq
blocs de compétences fondamentaux. Ils couvrent l'intégralité du cycle de vie de la
donnée dans un environnement Big Data : de la conception d'architectures de stockage
et de traitement des données massives (Blocs 1 et 2) à leur déploiement optimisé sur
le cloud (Bloc 3), en passant par l'application des méthodes d'intelligence
artificielle (Bloc 4) et l'élaboration de stratégies de gouvernance des données
(Bloc 5).

\needspace{10\baselineskip}
\begin{center}
\begin{tabularx}{\linewidth}{|p{2.6cm}|>{\raggedright\arraybackslash}p{4.3cm}|>{\raggedright\arraybackslash}X|}
\hline
\rowcolor{tableHeader}
\thead{Bloc} & \thead{Intitulé} & \thead{Compétences clés} \\
\hline
RNCP36739\newline Bloc 1 &
  Concevoir et développer une architecture de stockage de données &
  Bases de données relationnelles et non-relationnelles, construction de Datalake,
  création d'API d'accès aux données. \\
\hline
RNCP36739\newline Bloc 2 &
  Concevoir, développer et déployer une solution de traitement des données massives &
  Architectures distribuées, systèmes de streaming, optimisation des pipelines,
  automatisation CI/CD (conteneurisation, ordonnancement). \\
\hline
RNCP36739\newline Bloc 3 &
  Implémenter et optimiser des solutions de stockage et de traitement de données sur
  le cloud &
  Solutions cloud (stockage, pipelines), sécurisation (chiffrement, IAM, conformité),
  optimisation des performances et des coûts. \\
\hline
RNCP36739\newline Bloc 4 &
  Implémenter des méthodes d'intelligence artificielle pour modéliser et prédire de
  nouveaux comportements &
  Extraction et préparation des données, tableaux de bord interactifs, modèles
  prédictifs supervisés, évaluation des performances. \\
\hline
RNCP36739\newline Bloc 5 &
  Concevoir une stratégie de management et de gouvernance de données &
  Gouvernance (politiques, rôles), conformité (régulations, vie privée), audit
  qualité, stratégie de sécurité et d'accès aux données. \\
\hline
\end{tabularx}
\end{center}

\newpage
\section{Bloc 1 -- Concevoir et developper une architecture de stockage de données}

\subsection{Compétences visées}

\begin{itemize}
    \item Concevoir et développer une base de données relationnelle en réponse aux
          besoins d'un client en vue de la mise à disposition de ses données structurées,
          en utilisant les technologies et les langages de requêtes adaptés aux
          développements envisagés.
    \item Concevoir et développer une base de données non-relationnelle en vue de la
          mise à disposition de données semi-structurées et non structurées pour un
          traitement analytique ou d'intelligence artificielle, en utilisant les
          technologies et les langages de requêtes adaptés.
    \item Concevoir et construire un datalake en choisissant les architectures, les
          indicateurs de performance et les solutions de stockage appropriées afin
          d'intégrer des données provenant de systèmes d'information variés.
    \item Créer une API en utilisant les technologies permettant de rendre les données
          accessibles et d'augmenter l'efficacité et la praticité des applications et des
          services.
\end{itemize}

\subsection{Projets / TP associés}

\begin{itemize}
    \item Challenge Datalake 1 Data Integration
    \item Project Cloud
\end{itemize}

\subsection{Présentation des projets mobilisés}
\subsubsection{Challenge Datalake}

Pour ce bloc, nous nous appuyons principalement sur le projet \textit{Challenge Datalake}
réalisé en équipe. L'objectif était de construire une plateforme capable de produire une
vision consolidée du marché technologique européen en croisant plusieurs signaux :
popularité des technologies, demande des recruteurs, salaires, expérience
attendue et évolution temporelle. Le projet ne consistait donc pas seulement à
stocker des données, mais à mettre en place une chaîne complète capable de capter
des informations hétérogènes, de les fiabiliser puis de les restituer sous une
forme exploitable.

\paragraph{Problématique métier.}
L'idée directrice était de répondre à des questions simples en apparence, mais
complexes d'un point de vue data : quelles technologies montent ou reculent,
quelles compétences sont les plus demandées, dans quels pays les opportunités sont
les plus nombreuses, et quelles tendances peut-on observer entre la popularité
d'une technologie et sa présence dans les offres d'emploi. Pour cela, il fallait
rapprocher des sources qui n'avaient ni la même granularité, ni le même format,
ni le même rythme de mise à jour.

\paragraph{Sources de données mobilisées.}
Le projet reposait sur six sources principales, chacune apportant une lecture
complémentaire du marché.
\begin{itemize}
    \item \textit{Google Trends} fournissait des séries temporelles sur l'intérêt de recherche associé à différentes technologies.
    \item \textit{GitHub Repositories} permettait de mesurer la vitalité de l'écosystème open source à travers les dépôts, les langages, l'activité et la popularité des projets.
    \item \textit{StackOverflow Developer Survey} apportait des données d'enquête structurées sur les technologies utilisées, les profils développeurs, les salaires et les pratiques déclarées.
    \item \textit{Adzuna Jobs API} donnait accès à des offres d'emploi avec informations de salaire, de localisation et d'intitulé de poste.
    \item \textit{EuroTechJobs} complétait la couverture du marché européen via des offres récupérées par scraping.
    \item \textit{Jobicy} ajoutait une dimension \textit{remote-first}, utile pour capter les opportunités de travail à distance.
\end{itemize}
Cette diversité était un vrai enjeu technique : certaines sources étaient orientées
séries temporelles, d'autres jeux de données tabulaires annuels, d'autres encore
des flux d'annonces en JSON ou des pages web à parser. Nous avons donc travaillé à
la fois avec des API, des fichiers CSV, des structures semi-structurées et des
données issues de scraping.

\paragraph{Architecture générale du projet.}
Pour absorber cette hétérogénéité, nous avons conçu une architecture de type
\textit{Medallion} organisée en trois couches, implémentées dans des dossiers
techniques distincts du dépôt : \texttt{src/bronze}, \texttt{src/silver} et
\texttt{src/gold}. À cela s'ajoutait \texttt{src/api} pour l'exposition des
données et \texttt{src/utils} pour les composants transverses comme la gestion des
connexions SQL, des chemins, des schémas et de Spark. Le dépôt contient également
un script principal d'orchestration permettant d'exécuter le pipeline couche par
couche, ce qui rendait les traitements plus lisibles et plus faciles à rejouer.

\paragraph{Couche Bronze : ingestion et conservation du brut.}
La couche \textit{Bronze} avait pour rôle de collecter la donnée source sans la
transformer fortement. Chaque source disposait de son propre script d'import,
comme \texttt{import\_github\_repos.py}, \texttt{import\_stackoverflow\_survey.py},
\texttt{import\_eurotechjobs.py} ou \texttt{import\_jobicy.py}. L'idée était de
respecter au maximum la structure d'origine afin de conserver une trace fidèle des
données récupérées. Cette étape jouait le rôle de zone d'atterrissage du
datalake : elle facilitait le debug, la reprise sur incident et le reprocessing,
car nous pouvions repartir de la donnée brute sans relancer systématiquement les
collectes externes.

Dans la pratique, la couche Bronze n'était pas un simple dossier de dépôt. Elle
portait déjà des choix techniques importants : gestion des appels API,
temporisation pour limiter les erreurs de quota, sérialisation des données,
horodatage des extractions et sauvegarde dans des chemins standardisés. Sur le
flux GitHub par exemple, le script d'ingestion interroge l'API par langage ou par
thématique, calcule un premier score d'activité, puis écrit le résultat dans la
couche Bronze au format Parquet avec un nom de fichier daté. Cela donne une base
historisée, exploitable pour des traitements ultérieurs ou pour comparer plusieurs
runs d'ingestion.

\begin{lstlisting}[language=Python,caption={Extrait de la couche Bronze : sauvegarde d'une collecte GitHub dans la zone brute.}]
df = pd.DataFrame(repos_data)
df['created_at'] = pd.to_datetime(df['created_at'])
df['year'] = df['created_at'].dt.year
df['month'] = df['created_at'].dt.month

output_path = get_bronze_path('github_repos')
output_path.mkdir(parents=True, exist_ok=True)

filename = get_timestamped_filename("github_repos")
full_path = output_path / filename

df.to_parquet(full_path, engine='pyarrow', compression='snappy', index=False)
\end{lstlisting}

Cet extrait est représentatif de la philosophie Bronze : conserver un export brut
mais structuré, dans un format de fichier performant et relançable, sans encore
introduire toute la logique métier de normalisation.

\paragraph{Couche Silver : normalisation et fiabilisation.}
La couche \textit{Silver} correspondait au moment où la donnée devenait réellement
travaillable. Les scripts de transformation de cette couche, dédiés chacun à une
source ou à une famille de données, appliquaient des règles de nettoyage et
d'harmonisation. Cela incluait le renommage des colonnes, le typage, la
standardisation des pays et des dates, la préparation des champs de salaire, la
gestion des valeurs manquantes, la suppression des doublons et la mise en
cohérence des champs décrivant les technologies. Cette phase avait une importance
forte, car elle conditionnait la possibilité de comparer ensuite des données qui,
au départ, n'avaient pas le même vocabulaire ni le même niveau de détail.

La couche Silver jouait aussi un rôle de contrôle qualité. Pour certaines sources,
les scripts commençaient par relire l'ensemble des fichiers Parquet de la Bronze,
les concaténer, puis appliquer des traitements de déduplication et de
normalisation. Sur le flux GitHub, le traitement supprime les doublons sur
l'identifiant du dépôt, nettoie les noms, recalcule les métriques d'activité,
attribue un niveau d'activité et produit un score de qualité de donnée. Le résultat
est ensuite chargé dans MySQL avec une logique d'\textit{upsert}, afin de mettre à
jour les enregistrements déjà présents sans recréer inutilement toute la table.

\begin{lstlisting}[language=Python,caption={Extrait de la couche Silver : nettoyage, recalcul et préparation des données GitHub.}]
df = df.drop_duplicates(subset=['id'], keep='last').copy()
df.loc[:, 'name_cleaned'] = df['name'].str.strip().str.lower()
df.loc[:, 'technology_normalized'] = df['technology'].str.strip().str.lower()

for field in ['stars_count', 'forks_count', 'watchers_count']:
    df.loc[:, field] = pd.to_numeric(df[field], errors='coerce').fillna(0)

df = self._recalculate_metrics(df)
df.loc[:, 'processed_at'] = pd.Timestamp.now()
df.loc[:, 'data_quality_score'] = self._calculate_quality_score(df)
\end{lstlisting}

Cette étape est centrale dans un projet de datalake, car c'est elle qui fait
passer la donnée d'un état \og collecté \fg{} à un état \og exploitable \fg{}.
Sans cette standardisation intermédiaire, les jointures logiques entre sources et
les comparaisons analytiques resteraient trop fragiles.

\paragraph{Couche Gold : consolidation et vision analytique.}
La couche \textit{Gold} était portée par un travail d'unification, notamment via
le module \texttt{table\_unification.py}. Son objectif n'était plus seulement de
nettoyer les données, mais de produire des tables de consommation et des vues
analytiques. C'est à ce niveau que les différentes sources étaient rapprochées
autour de dimensions communes comme la technologie, le pays, la période ou le
type d'indicateur. Cette couche constituait la base des restitutions finales, car
elle proposait une information déjà consolidée, plus simple à interroger pour des
analyses transverses ou pour une exposition via API.

Techniquement, cette couche se rapprochait d'un entrepôt de données. Le script de
unification créait d'abord des tables de dimensions pour le calendrier, les pays,
les technologies, les entreprises et les sources de données, puis des tables de
faits dédiées à l'activité technologique et aux offres d'emploi détaillées. Cette
organisation en étoile avait un intérêt clair : fournir un modèle plus lisible
pour l'analyse croisée, où les sources hétérogènes deviennent des métriques
comparables autour de dimensions communes.

\begin{lstlisting}[language=Python,caption={Extrait de la couche Gold : création d'une table de faits orientée analyse.}]
'analysis_tech_activity': """
    CREATE TABLE IF NOT EXISTS analysis_tech_activity (
        id_activity BIGINT AUTO_INCREMENT PRIMARY KEY,
        date_key DATE NOT NULL,
        id_country INT,
        id_technology INT,
        id_source SMALLINT,
        job_count INT DEFAULT 0,
        avg_salary_usd DECIMAL(10,2),
        github_stars INT DEFAULT 0,
        search_volume INT DEFAULT 0
    ) ENGINE=InnoDB
"""
\end{lstlisting}

Ce type de structure montre bien la différence entre Silver et Gold. En Silver,
on fiabilise les tables source par source. En Gold, on change de niveau de
raisonnement : on construit des objets orientés décision, pensés pour l'analyse
et non plus seulement pour le nettoyage ou la conservation.

\paragraph{Choix de stockage.}
L'architecture s'appuyait sur une stratégie de stockage mixte. Les données
collectées et les jeux intermédiaires étaient conservés sous forme de fichiers
dans les répertoires du datalake, en particulier dans une logique de stockage brut
et de retraitement, avec des formats comme JSON, CSV ou Parquet selon les sources
et les traitements. En parallèle, les couches structurées \textit{Silver} et
\textit{Gold} étaient chargées dans MySQL, avec des bases dédiées. Ce découplage
est important : le stockage fichier servait la traçabilité, l'archivage et la
souplesse des pipelines, tandis que MySQL apportait une structure relationnelle
fiable pour la consultation, les filtres, les recherches et l'exposition des
données. Nous avons donc combiné deux logiques complémentaires plutôt que de
choisir un seul mode de stockage.

La structure technique globale du projet peut être résumée par le schéma suivant.

\begin{center}
    \begin{tikzpicture}[
            font=\small,
            >=Latex,
            box/.style={draw, rounded corners, align=center, minimum width=3.8cm, minimum height=1.2cm},
            smallbox/.style={draw, rounded corners, align=center, minimum width=2.8cm, minimum height=0.9cm},
            flow/.style={->, thick}
        ]
        \node[box, fill=green!15] (sources) {\textbf{Sources de données}\\Google Trends, GitHub,\\StackOverflow Survey, Adzuna,\\EuroTechJobs, Jobicy};
        \node[box, fill=orange!15, below=1.2cm of sources] (bronze) {\textbf{Bronze}\\Collecte brute\\API, fichiers, scraping\\Stockage fichiers / JSON / CSV / Parquet};
        \node[box, fill=blue!10, below=1.2cm of bronze] (silver) {\textbf{Silver}\\Nettoyage et normalisation\\PySpark / Pandas\\Stockage intermédiaire + MySQL silver};
        \node[box, fill=yellow!20, below=1.2cm of silver] (gold) {\textbf{Gold}\\Tables consolidées\\indicateurs et vues analytiques\\MySQL gold};
        \node[box, fill=purple!12, below=1.2cm of gold] (api) {\textbf{API REST}\\Django REST Framework\\filtres, pagination, documentation};

        \draw[flow] (sources) -- (bronze);
        \draw[flow] (bronze) -- (silver);
        \draw[flow] (silver) -- (gold);
        \draw[flow] (gold) -- (api);
    \end{tikzpicture}

    {\small \textit{Figure 1 : Synthèse de l'architecture technique du projet Challenge Datalake.}}
\end{center}

\paragraph{Traitements et outils techniques.}
Sur la partie traitement, nous avons mobilisé principalement Python, PySpark et
Pandas. Python servait de langage commun pour l'ingestion, l'orchestration et les
scripts métiers. PySpark intervenait pour traiter des jeux de données plus lourds
ou pour structurer les transformations dans une logique proche d'un pipeline big
data. Pandas était utilisé sur certains flux lorsque la volumétrie restait
raisonnable et qu'un traitement tabulaire classique suffisait. Cette combinaison
nous a permis d'adapter l'outil au type de source et au volume traité, tout en
gardant une cohérence d'ensemble dans le projet.

\paragraph{Organisation de l'API.}
Le dernier volet du projet concernait l'exposition de la donnée. Nous avons
développé une API REST avec Django et Django REST Framework afin de rendre les
résultats accessibles à d'autres applications ou à des utilisateurs métiers.
L'API était organisée en modules métiers par source, par exemple
\texttt{github\_jobs}, \texttt{stackoverflow\_survey}, \texttt{eurotechjobs},
\texttt{jobicy\_jobs}, \texttt{trends} et \texttt{adzuna\_jobs}, ainsi qu'un
module \texttt{analysis} pour les restitutions transverses. Cette structuration
reprenait la logique du pipeline : chaque source avait sa propre porte d'entrée,
mais l'API proposait aussi une lecture consolidée du marché.

\paragraph{Fonctionnalités d'exposition.}
Au-delà de la simple mise à disposition des tables, l'API proposait des fonctions
de filtre, de tri, de pagination et de documentation automatique via Swagger /
OpenAPI. Le projet comportait également des éléments d'authentification et de
gestion des permissions, ce qui montre que l'enjeu n'était pas seulement de sortir
des données, mais de construire une couche de service exploitable, documentée et
réutilisable. Dans une logique de démonstration, la collection Postman fournie
dans le dépôt permettait de tester rapidement les endpoints et de valider les cas
d'usage principaux.

\paragraph{Valeur du projet dans le rapport.}
Ce projet est particulièrement intéressant pour ce rapport, car il constitue un
cas concret d'architecture de données de bout en bout. Il relie des problématiques
de collecte, de structuration, de stockage, de transformation et de diffusion dans
une même chaîne technique. En d'autres termes, il ne s'agit pas d'un simple
exercice d'import de données, mais bien d'une plateforme cohérente où chaque
couche répond à un rôle précis, depuis la source brute jusqu'au service exposé aux
consommateurs.

Pour la sous-compétence NoSQL, nous mobilisons en complément un autre projet,
\textit{project\_cloud}, mais uniquement sur son volet NoSQL. Dans ce cadre, nous
avons utilisé Amazon DynamoDB pour concevoir une base non relationnelle orientée
cas d'usage, destinée à stocker rapidement des données semi-structurées issues de
plusieurs flux urbains sans imposer un schéma relationnel rigide.

\newpage
\subsection{Justification des compétences du bloc 1}

\subsubsection{Concevoir et développer une base de données relationnelle}

Le \textit{Challenge Datalake} démontre clairement cette compétence, car la couche
\textit{Silver} puis la couche \textit{Gold} reposent sur MySQL pour structurer et
exposer les données nettoyées. Dans ce projet, la base relationnelle ne sert pas
uniquement de zone de stockage ; elle joue le rôle de \textit{data warehouse}
centralisé, destiné à accueillir des données normalisées provenant de plusieurs
sources externes.

Le rapport du projet explique que la couche Bronze collecte les données brutes,
que la couche Silver les nettoie et les homogénéise, puis que la couche Gold les
unifie pour les rendre exploitables par l'API. Cette logique est confirmée dans
le dépôt \texttt{julientremont/Jobtech}, où la configuration Django et les
utilitaires SQL montrent l'usage explicite de MySQL. On y retrouve notamment des
schémas SQL dédiés, une gestion de connexions centralisée, ainsi qu'une
modélisation orientée analyse, avec des tables relationnelles adaptées aux
requêtes métier.

Cette partie répond donc à la compétence attendue, car il ne s'agissait pas
seulement d'installer une base SQL, mais bien de concevoir une structure
relationnelle cohérente avec le besoin client : centraliser des données sur le
marché de l'emploi tech, les rendre interrogeables, les relier entre elles et
permettre leur exploitation à travers une application. Le choix de MySQL est
également pertinent au regard de la nature des données structurées finales,
notamment pour les analyses croisées, les filtres, la pagination et les accès via
API.

\subsubsection{Concevoir et développer une base de données non relationnelle}

Cette sous-compétence est justifiée par un autre projet que nous avons réalisé,
\textit{project\_cloud}. Dans ce dépôt, la partie qui nous intéresse pour le bloc 1
est la conception de la couche NoSQL avec Amazon DynamoDB. L'objectif n'était pas
de reproduire un modèle relationnel, mais de stocker efficacement des données
hétérogènes liées à plusieurs domaines métier de la ville de Paris, notamment les
parkings, le trafic et les départs de transport.

Le modèle de données adopté est typiquement orienté document. Chaque enregistrement
est stocké sous forme d'item JSON dénormalisé, contenant directement les
informations utiles au cas d'usage. Par exemple, les données de parkings incluent
dans un même item l'identifiant, le nom, l'adresse, les capacités, les tarifs,
l'état courant, les coordonnées et l'horodatage d'ingestion ; les données de
trafic embarquent les propriétés métiers et les coordonnées du segment ; les
données de départs regroupent l'identifiant du trajet, la gare de départ, la
destination, le réseau, les horaires et la géolocalisation. Cette organisation est
adaptée à DynamoDB, car elle évite les jointures et privilégie un accès direct à
des objets déjà prêts à être consommés.

La modélisation repose sur des tables distinctes par grand flux métier, par exemple
\texttt{parkings}, \texttt{traffic} et \texttt{departures}. Ce découpage répond à une
logique d'usage : chaque table correspond à un type de données interrogé par des
traitements et des endpoints spécifiques. Les items partagent néanmoins certaines
métadonnées communes, notamment un champ \texttt{type}, un champ
\texttt{ingestion\_timestamp} et, selon les cas, des informations de localisation.
Ce choix permet d'unifier les traitements techniques tout en conservant la
souplesse propre à chaque domaine. Le code montre également l'usage d'écritures en
lot avec \texttt{batch\_writer()}, ce qui est cohérent avec une logique d'ingestion
fréquente et de chargement par lots.

Un point important de cette conception est la prise en compte des patrons d'accès.
Nous n'avons pas modélisé la base autour de relations théoriques entre entités,
mais autour des requêtes réellement attendues. Les scripts et l'API exploitent un
index secondaire global de type \textit{GSI} sur les attributs \texttt{type} et
\texttt{ingestion\_timestamp} afin de retrouver rapidement le dernier lot ingéré
ou les enregistrements correspondant à une fenêtre d'ingestion précise. Cette
stratégie est représentative d'une bonne pratique NoSQL : on part des besoins de
lecture, puis on construit la structure de stockage et les index en conséquence.

Cette conception répond à la compétence visée, car elle montre que nous avons su
adapter le modèle de stockage à des données semi-structurées et à des besoins
d'accès rapides, dans une logique orientée exploitation. Le volet NoSQL de
\textit{project\_cloud} montre concrètement notre capacité à choisir une
technologie non relationnelle lorsque la flexibilité du schéma, la dénormalisation,
la montée en charge et la rapidité de lecture priment sur les contraintes d'un
modèle SQL classique. C'est précisément ce qui était attendu ici : non pas
simplement utiliser DynamoDB, mais concevoir une base cohérente avec la nature des
données et les usages applicatifs visés.

\subsubsection{Concevoir et construire un datalake}

Le \textit{Challenge Datalake} est la partie la plus représentative de cette
compétence. L'architecture mise en place repose sur une logique \textit{Bronze /
    Silver / Gold}, explicitement décrite dans le rapport du projet. La couche Bronze
sert à collecter les données brutes depuis différentes sources externes et à les
stocker localement au format Parquet. La couche Silver applique les traitements de
nettoyage, de normalisation et d'enrichissement. Enfin, la couche Gold consolide
les données dans un entrepôt exploitable par l'API et par les analyses.

Le choix de cette architecture est pertinent car il permet de séparer clairement
les responsabilités : ingestion brute, préparation de la donnée et exposition des
données consolidées. Cette organisation facilite la maintenance, la traçabilité
et la reprise des traitements. Elle répond aussi à la diversité des systèmes
sources : API publiques, fichiers CSV, enquêtes annuelles, scraping web et données
issues de plateformes open source.

Le rapport de rendu détaille plusieurs flux : Google Trends avec collecte
historique et quotidienne, StackOverflow Survey avec import de fichiers annuels,
Adzuna avec appels API mensuels, EuroTech Jobs et Jobicy via scrapping, ainsi que
GitHub Repositories via API publique. Cette variété de formats et de rythmes
d'ingestion illustre bien la capacité à intégrer des données structurées,
semi-structurées et non structurées dans une architecture unique. Le stockage en
Parquet et le traitement avec PySpark montrent en outre une démarche orientée
performance et scalabilité, notamment grâce au partitionnement temporel et à la
standardisation progressive des colonnes.

Nous pouvons donc justifier cette compétence en montrant que le projet ne se limite pas
à une simple agrégation de fichiers : il s'agit bien d'un datalake conçu pour
absorber plusieurs sources, conserver la donnée brute, produire une donnée fiabilisée
et la rendre exploitable dans une couche analytique. C'est précisément l'objectif
attendu dans ce bloc.

\subsubsection{Créer une API pour rendre les données accessibles}

La dernière sous-compétence du bloc est également couverte par le
\textit{Challenge Datalake} grâce à l'API REST développée avec Django et Django
REST Framework. Le rapport du projet consacre une partie complète à cette API et
à son rôle de vitrine de données. L'objectif n'était pas seulement d'entreposer
les données, mais de les rendre accessibles de manière claire, documentée et
sécurisée.

L'API est organisée par source de données, avec des routes dédiées pour Google
Trends, StackOverflow Survey, Adzuna, GitHub, EuroTech Jobs et Jobicy, ainsi que
des endpoints d'analyse transverses pour la couche Gold. Le dépôt montre
également la présence d'une documentation Swagger / OpenAPI, d'une authentification
par session et JWT, d'un système de permissions, ainsi que de fonctionnalités de
filtrage, tri et pagination. Ces éléments sont importants, car ils traduisent une
conception orientée usage et non une simple exposition brute des tables.

Cette API justifie pleinement la compétence attendue : elle permet à des clients
ou à d'autres services d'interroger les données selon leurs besoins, de récupérer
des résumés, des séries temporelles ou des analyses métier, tout en conservant un
cadre technique robuste. Elle améliore donc concrètement l'accessibilité et la
praticité des données, ce qui correspond exactement à l'objectif de la sous-partie
\textit{API et Web Services}.

\subsection{Bilan du bloc 1}

Au travers de ces deux projets, nous pouvons justifier l'ensemble des sous-compétences
du bloc 1. Le \textit{Challenge Datalake} démontre notre capacité à concevoir une
architecture de stockage complète, à centraliser plusieurs flux de données, à
structurer une base relationnelle et à exposer les données via une API robuste.
Le projet \textit{project\_cloud} complète cette démonstration sur la partie NoSQL,
avec un cas concret d'usage de DynamoDB pour des données semi-structurées. Dans
l'ensemble, ces réalisations montrent notre capacité à choisir les bons modèles de
stockage selon les usages, à organiser les flux de données de bout en bout et à
mettre les données à disposition de manière exploitable.

\newpage
\section{Bloc 2 -- Concevoir, developper et deployer une solution de traitement des données massives}
\subsection{Compétences visées}

\begin{itemize}
    \item Concevoir, en s'appuyant sur une veille technologique, une architecture
          distribuée répondant au besoin du client pour traiter les données massives en
          entreprise en utilisant les technologies de traitement adaptées.
    \item Implementer un système distribué en utilisant des technologies de
          streaming identifiées à partir d'une veille pour traiter des données sur une
          période précise ou en temps quasi réel.
    \item Transformer les données provenant de différentes sources en prenant en
          compte leur variété pour faire de l'analytique à l'échelle.
    \item Optimiser la performance des pipelines en utilisant les techniques
          d'intégration et de mise en scène adéquates pour le traitement des données
          massives.
    \item Automatiser la création, les tests, l'intégration et le déploiement des
          pipelines de données en utilisant les technologies de containérisation et
          d'ordonnancement pertinentes.
\end{itemize}

\subsection{Projets / TP associes}

\begin{itemize}
    \item Projet Spark (Big Data Frameworks)
    \item DevOps \& MLOps
\end{itemize}

\subsection{Présentation des projets mobilisés}

\subsubsection{Projet Spark}

Pour le bloc 2, nous retenons en priorité le projet \textit{projet\_spark}, réalisé
dans le cadre du module Big Data Frameworks. Ce projet consistait à mettre en
place une pipeline de processing de données avec Apache Spark afin de croiser deux
types de données différentes : des données de vols aériens et des données météorologiques.
L'objectif général était de produire une base analytique permettant d'étudier
l'effet des conditions météorologiques sur les retards de vols, puis de générer
des indicateurs directement exploitables.

\paragraph{Contexte et objectif fonctionnel.}
Le cas d'usage retenu reposait sur un scénario d'analyse des performances du
transport aérien. Nous voulions répondre à des questions concrètes : quelles
compagnies cumulent les retards les plus importants, dans quelles conditions météo
les perturbations sont les plus marquées, et comment relier les événements météo à
des vols programmés à une heure et sur une zone donnée. Le projet s'inscrivait
donc pleinement dans une logique de traitement de données massives, car il fallait
intégrer des volumes importants de données de vols, les synchroniser avec des
séries météo horaires et produire des jeux de données prêts pour l'analyse.

\paragraph{Sources de données et variété des flux.}
Deux grandes familles de sources étaient mobilisées. La première concernait les
données de vols, lues depuis des fichiers CSV traités en mode streaming. La
seconde concernait les données météo historiques récupérées via l'API Open-Meteo
sur plusieurs points géographiques : aéroports majeurs comme Atlanta (ATL), Denver
(DEN), Los Angeles (LAX) et Chicago (ORD), mais aussi points intermédiaires sur certaines 
routes aériennes. Cette conception est intéressante, car elle combine un flux tabulaire 
volumineux et un flux externe API, avec une granularité horaire et géographique 
différente. Le projet devait donc gérer à la fois la volumétrie, la variété des schémas 
et l'alignement temporel.

\paragraph{Architecture distribuée avec Spark.}
L'ensemble du projet repose sur une architecture autour de Spark, nous avons installé
spark en local sur une machine puissante, le point d'entrée le cluster se fait via \texttt{spark://localhost:7077}. Les scripts d'ingestion, de streaming, de nettoyage, d'agrégation et de machine learning utilisent tous la même \texttt{SparkSession} où nous avons configuré la mémoire et le nombre de cœurs des CPU nécessaires pour le traitement.
L'architecture distribuée permet d'exécuter de nombreuses tâches en parallèle. Le dépôt est structuré selon une structure médaillon \textit{Bronze / Silver / Gold}, à laquelle s'ajoutent un entrepôt local pour la couche Bronze, un metastore Hive côté Silver et une sortie MariaDB pour la partie Gold. Cette organisation matérialise un pipeline distribué complet, où chaque étape a un rôle technique
précis.

Le schéma ci-dessous résume l'architecture technique du projet, depuis les deux
sources d'entrée jusqu'à l'analyse des données et la prédiction grâce à un modèle
de machine learning.

\begin{center}
    \includegraphics[width=0.98\textwidth]{assets/architecture.drawio.png}

    {\small \textit{Figure 2 : Vue d'ensemble technique du projet Spark.}}
\end{center}

\paragraph{Couche Bronze : ingestion batch et streaming.}
La première étape, la couche \textit{Bronze}, centralise les données brutes et correspond au point d'entrée technique du pipeline distribué. Elle reçoit des flux de natures différentes sans
chercher à les harmoniser immédiatement, ce qui permet de conserver une trace fidèle
des données réellement collectées. Dans notre projet, cette couche est alimentée par
deux mécanismes distincts. Le premier est un traitement batch, porté par
\texttt{ingest.py}, qui interroge l'API Open-Meteo pour récupérer des historiques
météo sur plusieurs localisations puis construit un DataFrame Spark avant de
persister les résultats en Parquet. Le second est un traitement streaming, porté par
\texttt{streaming.py}, qui lit les données de vols depuis des fichiers CSV déposés
dans un répertoire source et les intègre progressivement via \texttt{readStream}.

Ce choix technique est important, car il montre que la couche Bronze n'est pas
seulement un espace de dépôt passif. Elle constitue une zone d'atterrissage capable
d'absorber à la fois des extractions historiques et un flux incrémental. Le format
Parquet a été retenu pour cette couche brute car il reste compact, colonne par
colonne, tout en étant immédiatement exploitable par Spark pour des lectures
sélectives. Nous avons également mis en place le partitionnement des vols par \texttt{Year}, \texttt{Month} et \texttt{Day} cat il améliore les performances des traitements, puisqu'il permet de limiter les extractions de données à une période donnée au lieu de relire l'ensemble du jeu de données.

Nous avons aussi mis en place des mécanismes de robustesse dans la couche Bronze. Dans
le cas du streaming, le \textit{checkpointing} conserve l'état du flux et l'avancement
des micro-batches, ce qui permet une reprise plus fiable après interruption sans
retraiter arbitrairement tous les fichiers déjà vus. Cette approche est essentielle
dans un contexte Big Data, car elle sépare clairement la collecte de la logique
d'analyse. En cas d'erreur dans les transformations futures, nous pouvons rejouer les
traitements Silver et Gold à partir d'une donnée brute déjà stockée, sans devoir
réinterroger l'API météo ni reconstruire manuellement les entrées de vols.

\begin{lstlisting}[language=Python,caption={Extrait de la couche Bronze : ingestion streaming des vols vers des fichiers Parquet partitionnés.}]
flights = spark.readStream.format("csv").schema(schema) \
    .option("header", True).csv("source")

query = flights_year_month_day.writeStream \
    .format("parquet") \
    .partitionBy("Year", "Month", "Day") \
    .outputMode("append") \
    .option("path", abspath("bronze/flights")) \
    .option("checkpointLocation", abspath("bronze/checkpoints/flights")) \
    .start()
\end{lstlisting}

Sur la capture d'écran ci-dessous, on voit l'interface Spark Streaming qui permet de suivre l'état des traitements en cours : 

\begin{center}
    \includegraphics[width=0.98\textwidth]{assets/spark_streaming_ui.png}

    {\small \textit{Figure 3 : Capture d'écran de l'interface Spark Streaming.}}
\end{center}

\paragraph{Couche Silver : nettoyage, normalisation et mise en coherence.}
La couche \textit{Silver} est portée principalement par \texttt{cleaning.py}. C'est
à ce niveau que la donnée brute devient techniquement exploitable pour des jointures
et des calculs analytiques. Le rôle de cette couche n'est plus de simplement stocker
les entrées telles qu'elles arrivent, mais d'appliquer un cadre de qualité commun
aux deux flux. Pour les vols, cela commence par un recentrage du périmètre sur les
aéroports et les colonnes réellement utiles à l'étude. Cette réduction du bruit est
importante dans Spark, car elle diminue le volume de données manipulées dans les
étapes suivantes et évite de transporter des attributs sans valeur pour l'analyse.

Le travail principal de cette couche porte ensuite sur la normalisation temporelle.
Les heures de départ et d'arrivée sont d'abord converties en \texttt{timestamp}, puis
recalculées dans un référentiel commun en UTC à partir des fuseaux horaires des
aéroports d'origine et de destination. Cette étape est décisive : sans elle, un vol
et une observation météo pourraient être rapprochés alors qu'ils ne correspondent
pas au même instant réel. Le projet résout donc une difficulté classique des données
massives hétérogènes : rendre comparables des événements issus de systèmes qui n'ont
pas la même représentation du temps.

\begin{lstlisting}[language=Python,caption={Extrait de la couche Silver : normalisation des horaires de vol et conversion en UTC.}]
df = df.withColumn("OriginTimezone", mapping_expr[col("Origin")])
df = df.withColumn("DestTimezone", mapping_expr[col("Dest")])

df = df.withColumn(column, to_timestamp(col(column), "yyyy-MM-dd HH:mm"))
if column in ["CRSDepTime", "DepTime"]:
    df = df.withColumn(column, to_utc_timestamp(col(column), col("OriginTimezone")))
elif column in ["CRSArrTime", "ArrTime"]:
    df = df.withColumn(column, to_utc_timestamp(col(column), col("DestTimezone")))
\end{lstlisting}

La partie météo suit la même logique de fiabilisation. Les colonnes entièrement
nulles sont supprimées, les dates sont homogénéisées, puis les observations sont
converties dans un format temporel exploitable par Spark et alignées sur une
granularité horaire cohérente avec les heures de départ des vols. Une fois ces
transformations appliquées, les jeux de données ne sont plus simplement écrits
comme des fichiers Parquet dans un répertoire, comme dans la couche Bronze avec
\texttt{.save(...)}, mais sauvegardés comme de véritables tables Spark via
\texttt{saveAsTable(...)}. Dans \texttt{cleaning.py}, cette écriture est réalisée
après activation de \texttt{enableHiveSupport()} et configuration de
\texttt{spark.sql.warehouse.dir} vers \texttt{silver-warehouse}. Concrètement, les
données restent stockées physiquement sous forme de fichiers Parquet partitionnés
par \texttt{Year}, \texttt{Month} et \texttt{Day}, mais elles sont en plus
référencées dans le metastore Hive sous les noms logiques \texttt{flights} et
\texttt{weather}.

Le metastore Hive joue ici un rôle important : il conserve les métadonnées des
tables, c'est-à-dire leur schéma, leur emplacement physique, leurs partitions et
leur nom logique dans le catalogue Spark SQL. Cette couche de métadonnées apporte
une différence essentielle avec la Bronze : en Bronze, Spark relit un dossier de
fichiers bruts ; en Silver, il peut rouvrir directement des tables structurées et
stabilisées, déjà connues du moteur. Ce choix est particulièrement pertinent dans
notre projet, car la couche Gold peut s'appuyer sur ces tables intermédiaires pour
exécuter ses jointures et ses agrégations sans redéfinir les schémas, sans
manipuler explicitement les chemins de fichiers et sans rejouer toute la logique
de nettoyage. La persistance Silver constitue donc non seulement un stockage
intermédiaire, mais aussi une interface technique propre entre la préparation de
la donnée et son exploitation analytique.

\paragraph{Couche Gold : jointures analytiques et restitution.}
La couche \textit{Gold} s'appuie sur un script Spark SQL dédié. Elle correspond au
moment où l'on quitte une logique de préparation technique pour entrer dans une
logique de restitution analytique. Les tables Silver de vols et de météo y sont
croisées à l'aide de requêtes Spark SQL construites pour rapprocher deux dimensions
majeures du projet : la localisation et le temps. Concrètement, la jointure repose
sur l'aéroport d'origine du vol et sur une clé horaire dérivée de l'heure de départ,
ce qui permet d'associer à chaque vol les conditions météo les plus pertinentes au
moment où il est censé décoller.

Ce choix de modélisation est central pour la qualité des résultats. Une jointure
approximative sur la seule date ou sur une zone géographique trop large aurait
produit des indicateurs peu fiables. En imposant une granularité horaire et une
correspondance par localisation, le pipeline transforme deux flux indépendants en un
jeu d'analyse cohérent. La couche Gold peut alors produire des objets directement
consommables : retard moyen par compagnie, impact de la neige ou des précipitations,
vols les plus affectés par certaines conditions météo, ou encore ratios de retards
par transporteur.

Sur le plan technique, Spark SQL n'apporte pas seulement une écriture plus lisible
des requêtes analytiques : il fournit aussi une couche d'exécution unifiée entre
le modèle relationnel exprimé par les requêtes et le moteur distribué de Spark.
Les jointures entre \texttt{flights} et \texttt{weather}, ainsi que les agrégations
sur les retards, sont formulées sous forme déclarative, puis traduites en plan
d'exécution par Spark. Cela permet de bénéficier des optimisations du moteur,
notamment sur les projections, les filtres, les phases de jointure et les
regroupements, tout en gardant une écriture proche du SQL classique. Dans notre
projet, cette approche était particulièrement adaptée, car elle permettait de
produire plusieurs tables analytiques spécialisées, comme le retard moyen par
compagnie, les vols les plus impactés pendant un épisode neigeux ou les retards
maximaux par type de météo, à partir des mêmes tables Silver, sans dupliquer la
logique de préparation.

\begin{lstlisting}[language=Python,caption={Extrait de la couche Gold : jointure analytique entre vols et conditions météo.}]
query = """
    SELECT f.Airline, f.Origin, f.Dest, f.ArrDelayMinutes,
           w.snowfall, w.wind_speed_10m, w.precipitation, w.weather_code
    FROM flights as f
    LEFT JOIN weather as w
      ON f.Origin = w.location
     AND f.CRSDepTime_hourly = w.datetimeUTC
    WHERE w.weather_code IN (71,73,75)
    ORDER BY f.ArrDelayMinutes DESC
"""
df = spark.sql(query)
\end{lstlisting}

Les résultats finaux ne restent toutefois pas dans Spark. Une fois calculés, ils
sont exportés vers MariaDB via le connecteur JDBC, ce qui transforme les sorties
Gold en tables relationnelles persistantes, directement interrogeables par des
outils externes. Cette étape est importante sur le plan architectural : Spark joue
le rôle de moteur de calcul, tandis que MariaDB joue le rôle de service layer.
Les consommateurs n'ont donc pas besoin d'accéder au cluster Spark, ni de
relancer les scripts de traitement, pour exploiter les indicateurs produits. Dans
notre cas, cette base MariaDB était hébergée sur un Raspberry Pi, ce qui
permettait d'exposer les tables Gold sur le réseau et de les rendre accessibles en
ligne depuis d'autres postes.

\begin{center}
    \includegraphics[width=0.75\textwidth]{assets/spark-mysql-table.png}

    {\small \textit{Figure 4 : Exemple de table dans MariaDB}}
\end{center}

Ce choix a ensuite facilité l'intégration avec Power BI. Plutôt que de connecter
l'outil de visualisation directement aux scripts Spark ou aux fichiers
intermédiaires, nous avons pu brancher Power BI sur des tables déjà calculées,
structurées et stabilisées dans MariaDB. Cette séparation entre calcul distribué
et restitution présente plusieurs avantages : elle simplifie la consommation des
données, réduit le couplage avec l'environnement Spark, et permet à un outil BI
de travailler sur une source relationnelle standard, accessible à distance. Autrement
dit, la couche Gold ne produit pas uniquement des résultats analytiques ; elle
alimente aussi une interface de diffusion exploitable pour la visualisation, le
reporting et la consultation en ligne.

\begin{center}
    \includegraphics[width=0.98\textwidth]{assets/powerbi-spark-1.png}

    {\small \textit{Figure 5 : Dashboard Power BI}}
\end{center}

\begin{center}
    \includegraphics[width=0.98\textwidth]{assets/powerbi-spark-2.png}

    {\small \textit{Figure 6 : Dashboard Power BI}}
\end{center}

\paragraph{Dimension machine learning.}
Le projet inclut également un script \texttt{ML.py}, qui prolonge la chaîne de
traitement par une étape de modélisation avec \texttt{pyspark.ml}. Les données de
vols et de météo y sont fusionnées, transformées en variables numériques,
indexées, standardisées puis injectées dans une régression logistique avec
validation croisée. Cette partie n'est pas le coeur de la démonstration du bloc 2,
mais elle montre que la chaîne Spark construite est suffisamment robuste pour
servir ensuite à des traitements avancés sur données préparées.

\subsubsection{Projet DevOps : fizzbuzz-Efrei}

En complément du projet Spark, nous mobilisons également le dépôt
\texttt{fizzbuzz-Efrei} pour justifier la dimension DevOps de ce bloc. Le périmètre
fonctionnel de l'application est volontairement simple : il s'agit d'une
implémentation Python du problème FizzBuzz accompagnée de tests unitaires. Ce choix
est justement intéressant dans le cadre du bloc 2, car il permet de concentrer la
démonstration non pas sur la complexité métier, mais sur l'industrialisation du cycle
de livraison : contrôle qualité, intégration continue, conteneurisation et préparation
au déploiement.

Le premier apport du projet concerne la chaîne d'intégration continue mise en place
avec GitHub Actions. Le dépôt contient un workflow \texttt{python-package.yml}
déclenché sur les branches \texttt{dev}, \texttt{main} et \texttt{docker}. À chaque
push, ce pipeline exécute automatiquement la même suite de vérifications sur une
matrice de versions Python \texttt{3.9}, \texttt{3.10} et \texttt{3.11}. Les étapes
enchainent l'installation des dépendances, l'analyse statique avec \texttt{flake8},
l'exécution des tests unitaires via \texttt{python -m unittest test.py}, puis la
mesure de couverture avec \texttt{coverage run} et \texttt{coverage report}. Cette
organisation montre que nous avons mis en place une validation systématique du code
avant intégration, afin de détecter rapidement les régressions, de garantir la
compatibilité sur plusieurs environnements Python et de fiabiliser les livraisons.

Le second apport majeur du projet est la conteneurisation de l'application. Le
\texttt{Dockerfile} s'appuie sur une image \texttt{python:3.12.8-slim}, définit un
répertoire de travail dédié, installe les dépendances depuis \texttt{requirements.txt}
et exécute l'application avec un utilisateur non privilégié. Ce dernier point est
important, car il traduit une prise en compte des bonnes pratiques de sécurité dès la
phase de packaging. Le dépôt contient également un fichier \texttt{compose.yaml} qui
permet de reconstruire puis de lancer localement le service avec
\texttt{docker compose up --build}. Nous obtenons ainsi un environnement d'exécution
reproductible, indépendant de la machine du développeur, ce qui réduit fortement les
écarts entre développement, validation et livraison.

Le projet ne montre pas un déploiement de production complet avec orchestrateur ou
infrastructure cloud, et il ne faut pas le présenter comme tel. En revanche, il montre
très clairement la mise en place des briques attendues dans une démarche CI/CD : une
application validée automatiquement, empaquetée sous forme d'image Docker et prête à
être publiée dans un registre puis déployée dans un environnement cible. Le
\texttt{README.Docker.md} documente d'ailleurs explicitement les commandes de build et
de push d'image. Dans le cadre du bloc 2, cette réalisation est pertinente car elle
démontre notre capacité à automatiser la vérification du code, à standardiser
l'exécution de l'application et à préparer une chaîne de déploiement outillée,
complémentaire des enjeux de traitement massif couverts par Spark.

\newpage
\subsection{Analyse attendue}

Mettez en avant la conception des pipelines, le traitement distribue, les choix
de performance, l'automatisation et la maniere dont vous avez deploie ou
industrialise les traitements.

\subsubsection{Preuves d'acquisition des compétences}

Le tableau suivant récapitule, pour chacune des cinq sous-compétences du Bloc 2, la
réalisation concrète qui la démontre dans le projet.

\needspace{14\baselineskip}
\begin{center}
\begin{tabularx}{\linewidth}{|>{\raggedright\arraybackslash}p{4.2cm}|>{\raggedright\arraybackslash}X|}
\hline
\rowcolor{tableHeader}
\thead{Compétence visée} & \thead{Réalisation et preuve dans le projet} \\
\hline
Architecture distribuée pour le traitement de données massives &
  Architecture Spark distribuée structurée en couches Bronze / Silver / Gold,
  pilotée par une \texttt{SparkSession} commune et un point d'entrée cluster
  \texttt{spark://localhost:7077}. \\
\hline
Système distribué de streaming &
  Ingestion incrémentale des vols via \texttt{readStream}, écriture en mode
  \texttt{append}, partitionnement des sorties et \textit{checkpointing} pour la
  reprise fiable des micro-batches. \\
\hline
Transformation de données variées pour l'analytique à l'échelle &
  Croisement de deux sources hétérogènes (vols CSV et météo API), normalisation
  temporelle en UTC en Silver, puis jointures analytiques en Gold avec Spark SQL. \\
\hline
Optimisation des performances des pipelines &
  Utilisation du format Parquet, partitionnement par \texttt{Year}/\texttt{Month}/\texttt{Day},
  séparation Bronze / Silver / Gold et exécution distribuée Spark SQL pour limiter
  les lectures inutiles et accélérer les agrégations. \\
\hline
Automatisation, tests, intégration et déploiement des pipelines &
  Workflow GitHub Actions exécuté sur plusieurs branches et versions Python,
  combinant linting \texttt{flake8}, tests \texttt{unittest}, couverture
  \texttt{coverage}, puis conteneurisation avec Docker et exécution reproductible via
  \texttt{docker compose} pour préparer la livraison de l'application. \\
\hline
\end{tabularx}
\end{center}

\newpage
\section{Bloc 3 -- Implementer et optimiser des solutions de stockage et de traitement de données sur le cloud}

Cette partie démontre l’acquisition des quatre compétences du Bloc 3 du référentiel
RNCP36739~\cite{rncp} à travers une plateforme \textbf{cloud serverless AWS}. Chaque
étape couvre une ou plusieurs sous-compétences du référentiel, synthétisées dans le
tableau de validation a la fin du bloc.

\subsection{Compétences visées}

Le référentiel RNCP36739 définit quatre sous-compétences pour le Bloc 3, que ce
dossier couvre intégralement à travers le \texttt{projet cloud}  Ce bloc porte sur
la mise en œuvre de solutions cloud pour le stockage, le traitement et la mise à
disposition des données : choix des services managés, conception des flux, sécurisation
des accès, optimisation de la performance et de la disponibilité

\begin{itemize}[leftmargin=1.4em, itemsep=3pt, topsep=3pt]
    \item Mettre en \oe{}uvre des solutions de stockage de données dans le cloud pour
          permettre aux entreprises d'explorer leurs données en implémentant des
          techniques et stratégies adaptées à leur utilisation.
    \item Concevoir et développer des pipelines et des solutions de traitement de
          données dans le cloud en chargeant les données, en les transformant et en les
          mettant à disposition des utilisateurs.
    \item Mettre en place une politique de sécurité des données dans le cloud en
          développant une stratégie de chiffrement et de gestion des identités et des
          accès, dans le respect des règlements en vigueur.
    \item Optimiser les solutions de stockage et de traitement des données dans le
          cloud en définissant des indicateurs de performance pour assurer la
          disponibilité des services et optimiser les coûts.
\end{itemize}

\subsection{Projets / TP associes}

\begin{itemize}
    \item Projet Cloud
    \item Securite des données
\end{itemize}

\subsection{Contexte et enjeux}

Le \texttt{projet cloud} est une plateforme de données temps réel lyonnaise. Elle
agrège en continu trois flux de données ouvertes : les départs ferroviaires et de
métro en gare de Lyon (SNCF/RATP), l'état des parkings parisiens (disponibilité,
places, tarifs) et l'état du trafic routier (indice de congestion, segments
critiques, fluidité par zone). L'objectif est de permettre un suivi opérationnel de
la mobilité lyonnaise via un tableau de bord Streamlit alimenté par une API
serverless.

L'enjeu architectural central est double : garantir la latence sur des données en
temps réel et maîtriser les coûts. Un scan DynamoDB complet pour chaque
requête API serait fonctionnellement correct, mais son coût de lecture croîtrait
linéairement avec le volume. La mise en place d'un Global Secondary Index (GSI)
ramène le coût de chaque requête à O(1), quelle que soit la taille de la table.

\subsubsection{Positionnement dans l'écosystème AWS}

AWS propose plusieurs alternatives pour chaque composant de l'architecture. Les
choix retenus sont justifiés par rapport à ces alternatives.

\needspace{8\baselineskip}
\begin{center}\begin{tabularx}{\linewidth}{|l|l|l|>{\raggedright\arraybackslash}X|}
\hline
\rowcolor{tableHeader}
\thead{Besoin} & \thead{Choix retenu} & \thead{Alternative} & \thead{Justification} \\
\hline
Lac de données & S3 & EFS, EBS & Coût minimal, accès objet universel \\
\hline
Base NoSQL & DynamoDB & MongoDB Atlas, ElasticSearch & Managé, serverless, GSI natif \\
\hline
Compute API & Lambda & EC2, ECS/Fargate & Pas d'infra à gérer, facturation à l'usage \\
\hline
Routage API & API Gateway & ALB, CloudFront & Intégration Lambda native, gestion CORS \\
\hline
Dashboard & Streamlit (EC2) & QuickSight, PowerBI & Open source, Python natif \\
\hline
Scheduling & EventBridge & Step Functions, MWAA & Cron simple, coût négligeable \\
\hline
\end{tabularx}\end{center}

\subsection{Architecture cloud}

L'architecture suit le motif Medallion (Bronze/Silver/Gold) transposé sur des
services managés AWS : S3 comme data lake brutes, DynamoDB comme couche
nettoyée puis agrégée, et une exposition serverless via Lambda et API Gateway.

\needspace{20\baselineskip}
\begin{center}\fitwidth{%
\begin{tikzpicture}[node distance=0.85cm]

% Sources
\node[minibox, fill=black!5, draw=black!30, text width=3cm] (sncf)
  {\small\textbf{API SNCF/RATP}\\Départs};
\node[minibox, fill=black!5, draw=black!30, text width=3cm, right=0.6cm of sncf] (park)
  {\small\textbf{API Parkings}\\Disponibilité};
\node[minibox, fill=black!5, draw=black!30, text width=3cm, right=0.6cm of park] (traf)
  {\small\textbf{API Trafic}\\Congestion};

% Bronze S3
\node[medalbox, fill=bronzeBg, draw=bronzeColor, below=0.9cm of park] (s3) {
  \textbf{\textcolor{bronzeColor}{BRONZE}} \quad AWS S3 — bucket \texttt{efrei-cloud-project}\\[2pt]
  \small \texttt{departures|parking|traffic/<timestamp\_ISO>/data.json}
};

% Silver DynamoDB
\node[medalbox, fill=silverBg, draw=silverColor, below=0.8cm of s3] (ddb) {
  \textbf{\textcolor{silverColor}{SILVER}} \quad DynamoDB — 3 tables temps réel\\[2pt]
  \small \texttt{parkings} · \texttt{traffic} · \texttt{departures} \quad · \quad GSI : (type, ingestion\_timestamp) \textbf{O(1)}
};

% Gold DynamoDB agrégats
\node[medalbox, fill=goldBg, draw=goldColor, below=0.8cm of ddb] (gold) {
  \textbf{\textcolor{goldColor}{GOLD}} \quad DynamoDB — 10 tables d'agrégats\\[2pt]
  \small occupancy\_rate · availability · congestion\_index · fluidity · departures\_by\_network …
};

% API
\node[flowbox, fill=cloudBg, draw=cloudColor, below=0.8cm of gold, text width=9cm] (api) {
  \textbf{\textcolor{cloudColor}{Lambda + API Gateway}} — 13 endpoints\\[2pt]
  \small 3 temps réel (GSI query) · 10 agrégats (scan statique) · Streamlit Dashboard
};

\draw[arrowM] (sncf.south) -- ++(0,-0.35) -| ([xshift=-2cm]s3.north);
\draw[arrowM] (park.south) -- (s3.north);
\draw[arrowM] (traf.south) -- ++(0,-0.35) -| ([xshift=2cm]s3.north);
\draw[arrowM] (s3) -- node[right, font=\scriptsize\itshape, text=black!50] {\texttt{process\_s3\_to\_dynamodb.py}} (ddb);
\draw[arrowM] (ddb) -- node[right, font=\scriptsize\itshape, text=black!50] {\texttt{agregation\_*.py}} (gold);
\draw[arrowM] (gold) -- (api);

\end{tikzpicture}}\end{center}

Le schéma technique du projet ci-dessous détaille le flux complet, du calcul
ordonnancé sur EC2 jusqu'à la visualisation Streamlit.

\figc{fig_architecture_cloud.png}{\linewidth}{Architecture technique du
\texttt{projet cloud}. Les trois sources open data sont collectées par un pipeline
ordonnancé sur EC2, stockées en JSON dans S3, nettoyées et indexées dans DynamoDB,
agrégées dans un rapport quotidien, puis exposées via une fonction Lambda et une API
Gateway consommée par le dashboard Streamlit.}

\subsection{Stockage S3 --- data lake brutes horodaté}

AWS S3~\cite{s3} constitue la couche Bronze de l'architecture. Chaque ingestion
produit un objet JSON horodaté dans le préfixe correspondant à la source
(\texttt{departures/<timestamp\_ISO>/data.json}). Cette organisation préserve
l'historique complet des snapshots et permet de rejouer n'importe quelle fenêtre
temporelle sans perte.

\figc{fig_s3_tree.png}{\linewidth}{Organisation du bucket S3
\texttt{efrei-cloud-project} : un préfixe par source (départs, parking, trafic) et,
sous chacun, un dossier horodaté par snapshot contenant le fichier \texttt{data.json}.}

\begin{lstlisting}[language=Python, caption=Écriture et lecture S3 (utils\_s3.py)]
def upload_to_s3(s3, json_data, bucket_name, key):
    s3.put_object(
        Bucket=bucket_name,
        Key=key,
        Body=json.dumps(json_data, indent=4, cls=DecimalEncoder)
    )

def get_json_from_s3(s3, bucket, key):
    response = s3.get_object(Bucket=bucket, Key=key)
    content  = response["Body"].read().decode("utf-8")
    return json.loads(content)

def get_s3_object_keys(s3, bucket_name, prefix):
    response = s3.list_objects_v2(Bucket=bucket_name, Prefix=prefix)
    return [obj['Key'] for obj in response.get('Contents', [])]
\end{lstlisting}

Le \texttt{DecimalEncoder} sérialise les \texttt{Decimal} Python en \texttt{int} ou
\texttt{float} selon la présence d'une partie décimale --- une nécessité, car
DynamoDB stocke tous les nombres au format \texttt{Decimal}.

\capture{Console AWS S3 -- bucket \texttt{efrei-cloud-project}, dossier \texttt{departures} : liste des objets JSON horodatés.}

\subsection{DynamoDB --- modèle d'accès et index secondaire global}

\subsubsection{Modèle de données}

Chaque item DynamoDB~\cite{dynamodb} dans les trois tables temps réel porte un
attribut \texttt{type} (valeur constante par table : \texttt{"parkings"},
\texttt{"traffic"}, \texttt{"departures"}) et un \texttt{ingestion\_timestamp} en
microsecondes Unix. Ce couple constitue la clé du GSI. L'horodatage est exprimé en
microsecondes (\texttt{timestamp() * 1\_000\_000}) pour garantir l'unicité sur des
ingestions rapprochées : un horodatage en secondes provoquerait des collisions si
deux snapshots arrivaient dans la même seconde.

\begin{lstlisting}[language=Python, caption=Création d'un item avec horodatage microseconde (process\_s3\_to\_dynamodb.py)]
def create_stations_departures_dict(json_data, ingestion_timestamp_str):
    results = []
    for index, departure in enumerate(json_data["departures"]):
        result = {
            "gid": index,
            "trip_id": departure["display_informations"]["trip_short_name"],
            "ingestion_timestamp": int(
                datetime.fromisoformat(ingestion_timestamp_str).timestamp()
                * 1_000_000),           # microsecondes : unicite garantie
            "departure_datetime": departure["stop_date_time"]["base_departure_date_time"],
            "departure_station":  departure["stop_point"]["name"],
            "network":            departure["display_informations"]["network"],
            "type": "departures",       # PK du GSI : valeur constante par table
            "long": departure["stop_point"]["coord"]["lon"],
            "lat":  departure["stop_point"]["coord"]["lat"],
        }
        results.append(result)
    return results
\end{lstlisting}

\subsubsection{Global Secondary Index --- requête O(1) sur le dernier snapshot}

L'enjeu de performance est d'accéder au dernier snapshot \textit{sans} scanner toute
la table. Sans index, chaque requête \og{}dernier état des parkings\fg{} serait un
\texttt{scan} en O(n) sur l'ensemble de l'historique. Avec le GSI
\texttt{gsi-type-ingestion\_timestamp} (PK : type, SK : ingestion\_timestamp), une
\texttt{query} avec \texttt{ScanIndexForward=False, Limit=1} retourne
l'enregistrement le plus récent en O(1).

\needspace{8\baselineskip}
\begin{center}\begin{tabularx}{\linewidth}{|l|>{\raggedright\arraybackslash}X|>{\raggedright\arraybackslash}X|}
\hline
\rowcolor{tableHeader}
\thead{Approche} & \thead{Sans GSI (scan)} & \thead{Avec GSI (query)} \\
\hline
Items lus & Toute la table (N) & Exactement 1 \\
\hline
Unités de lecture (RCU) & N & 1 \\
\hline
Complexité & O(n) & O(1) \\
\hline
Tri & Côté application & Natif (sort key) \\
\hline
\end{tabularx}\end{center}

\begin{lstlisting}[language=Python, caption=Requête GSI -- dernier timestamp O(1) avec fallback scan (utils\_dynamodb.py)]
def get_last_processed_timestamp(dynamodb, table_name, type_table):
    table = dynamodb.Table(table_name)
    try:
        response = table.query(
            IndexName='gsi-type-ingestion_timestamp',
            KeyConditionExpression=Key('type').eq(type_table),
            ScanIndexForward=False,   # tri SK descendant : le plus grand en tete
            Limit=1,
            ProjectionExpression="ingestion_timestamp"
        )
        items = response.get('Items', [])
        return items[0]['ingestion_timestamp'] if items else None
    except Exception:
        # Fallback : scan si GSI non disponible (degraded mode)
        response = table.scan(
            ProjectionExpression="ingestion_timestamp", Limit=1000)
        timestamps = [item['ingestion_timestamp'] for item in response['Items']]
        return max(timestamps) if timestamps else None
\end{lstlisting}

Le fallback sur \texttt{scan} garantit la disponibilité du service même si le GSI est
en cours de construction ou temporairement indisponible (mode dégradé). Ce même
pattern --- query GSI en priorité, scan en secours --- est réutilisé dans le Lambda
de l'API.

\capture{Console AWS DynamoDB -- table \texttt{parkings}, onglet \textit{Indexes} : configuration du GSI \texttt{gsi-type-ingestion\_timestamp}.}

\subsubsection{Ingestion incrémentale depuis S3}

Le pipeline S3 $\rightarrow$ DynamoDB ne retraite que les nouveaux objets S3. La
borne de reprise est le dernier \texttt{ingestion\_timestamp} connu via le GSI ; les
clés S3 sont filtrées par comparaison ISO de leur horodatage.

\begin{lstlisting}[language=Python, caption=Ingestion incrémentale S3 vers DynamoDB (process\_s3\_to\_dynamodb.py)]
last_processed_timestamp_str = get_last_processed_timestamp(
    dynamodb, DYNAMODB_TABLE_NAME, "departures")

keys     = get_s3_object_keys(s3, BUCKET_NAME, s3_folder)
new_keys = []

if last_processed_timestamp_str:
    last_dt = datetime.fromtimestamp(
        float(last_processed_timestamp_str) / 1_000_000)
    for key in keys:
        key_dt = datetime.fromisoformat(key.split('/')[1])
        if key_dt > last_dt:
            new_keys.append(key)
else:
    new_keys = keys    # premiere ingestion : tout traiter

for key in new_keys:
    response = get_json_from_s3(s3, BUCKET_NAME, key)
    items    = create_stations_departures_dict(response, key.split('/')[1])
    batch_put_items_to_dynamodb(dynamodb, DYNAMODB_TABLE_NAME, items)
\end{lstlisting}

L'idempotence est garantie : à chaque relance, seuls les objets S3 postérieurs au
dernier horodatage connu sont réingérés, sans duplication des données déjà présentes.

\subsection{Traitement cloud --- agrégation analytique}

Un pipeline d'agrégation lit les tables temps réel (Silver) et produit dix tables
analytiques pré-agrégées dans DynamoDB (Gold). Pour les parkings, l'agrégation se
fait par tranche horaire (groupement sur \texttt{ingestion\_timestamp //
3\_600\_000\_000}), puis calcul des indicateurs métier.

\needspace{12\baselineskip}
\begin{center}\begin{tabularx}{\linewidth}{|l|>{\raggedright\arraybackslash}X|}
\hline
\rowcolor{tableHeader}
\thead{Table DynamoDB (Gold)} & \thead{Indicateur} \\
\hline
\texttt{aggregation\_overall\_occupancy\_rate} & Taux d'occupation global (\%) \\
\hline
\texttt{aggregation\_average\_availability\_parking} & Disponibilité moyenne par parking \\
\hline
\texttt{aggregation\_reference\_pricing} & Tarification moyenne (1h/2h/4h/24h) \\
\hline
\texttt{aggregation\_number\_of\_parkings\_in\_operation} & Nombre de parkings ouverts \\
\hline
\texttt{aggregation\_traffic\_congestion\_index} & Indice de congestion du trafic \\
\hline
\texttt{aggregation\_traffic\_critical\_segments} & Segments routiers critiques \\
\hline
\texttt{aggregation\_traffic\_fluidity\_by\_zone} & Fluidité par zone géographique \\
\hline
\texttt{aggregation\_departures\_by\_network} & Départs par réseau (SNCF/RATP) \\
\hline
\texttt{aggregation\_departures\_top\_destinations} & Top destinations \\
\hline
\texttt{aggregation\_departures\_total} & Total des départs sur la période \\
\hline
\end{tabularx}\end{center}

Ce pré-calcul transfère la charge CPU du moment de la requête vers le moment de
l'ingestion : l'API lit ensuite ces tables d'agrégats (petites et statiques) sans
recalculer quoi que ce soit à chaque appel.

\subsection{Mise à disposition --- Lambda et API Gateway}

\subsubsection{Routage des 13 endpoints serverless}

L'API expose treize endpoints via un Lambda~\cite{lambda} unique routé par chemin :
trois endpoints temps réel (requête GSI vers le dernier snapshot) et dix endpoints
d'agrégats (scan de tables statiques).

\begin{lstlisting}[language=Python, caption=Lambda handler -- routage des 13 endpoints (api/main.py)]
def lambda_handler(event, context):
    dynamodb = boto3.resource('dynamodb')
    path     = event.get('path')

    # Endpoints agregats : scan complet (tables fixes, volume faible)
    aggregation_tables = [
        "aggregation_overall_occupancy_rate",
        "aggregation_average_availability_parking",
        "aggregation_reference_pricing",
        "aggregation_traffic_congestion_index",
        "aggregation_departures_by_network",
        # ... 10 tables au total
    ]
    if path.lstrip('/') in aggregation_tables:
        items = get_all_items(dynamodb, path.lstrip('/'))
        return {'statusCode': 200,
                'body': json.dumps(items, default=decimal_serializer)}

    # Endpoints temps reel : requete GSI -> dernier snapshot
    if path == '/parkings':     table_name, type_value = "parkings",   "parkings"
    elif path == '/traffic':    table_name, type_value = "traffic",    "traffic"
    elif path == '/departures': table_name, type_value = "departures", "departures"
    else:
        return {'statusCode': 404,
                'body': json.dumps({'message': f'Not Found {path}'})}

    last_ts = get_last_ingestion_time(dynamodb, table_name, type_value)
    items   = get_items_by_ingestion_time(dynamodb, table_name, last_ts, type_value)
    return {'statusCode': 200,
            'body': json.dumps(items, default=decimal_serializer)}
\end{lstlisting}

L'architecture serverless (Lambda + API Gateway~\cite{apigateway}) supprime toute
gestion d'infrastructure : pas de serveur à provisionner, pas de scaling manuel, et
une facturation à la requête --- optimale pour un flux temps  réel à appels
intermittents.

\subsubsection{Cold start et stratégies de mitigation}

Une invocation Lambda comprend deux phases : l'initialisation (\textit{cold start})
et l'exécution. Le cold start n'a lieu qu'à la première invocation après une période
d'inactivité ; pour un Lambda Python avec boto3, il est typiquement de 200 à 500\,ms.

\needspace{8\baselineskip}
\begin{center}\begin{tabularx}{\linewidth}{|l|l|>{\raggedright\arraybackslash}X|}
\hline
\rowcolor{tableHeader}
\thead{Phase} & \thead{Durée typique} & \thead{Fréquence} \\
\hline
Cold start (init) & 200--500 ms & Première invocation, après inactivité \\
\hline
Warm execution & 10--100 ms & Invocations suivantes (même conteneur) \\
\hline
\end{tabularx}\end{center}

Pour un tableau de bord rafraîchi toutes les 5 minutes, le cold start est acceptable.
Pour une API à très faible latence, trois leviers existent : la \textbf{Provisioned
Concurrency} (instances maintenues chaudes en permanence), un \textbf{warm-up ping}
EventBridge, ou un package minimal (boto3 étant fourni par le runtime AWS, le ZIP ne
contient que le code applicatif ce qui réduit le temps de chargement).

\capture{Console AWS API Gateway -- liste des 13 routes configurées (méthode GET + intégration Lambda).}

\subsection{Sécurité et gestion des identités}

\subsubsection{Principe du moindre privilège (IAM)}

Chaque composant s'exécute sous un rôle IAM dédié n'accordant que les permissions
strictement nécessaires à sa fonction.

\needspace{8\baselineskip}
\begin{center}\begin{tabularx}{\linewidth}{|l|>{\raggedright\arraybackslash}p{4cm}|>{\raggedright\arraybackslash}X|}
\hline
\rowcolor{tableHeader}
\thead{Rôle IAM} & \thead{Permissions} & \thead{Justification} \\
\hline
\texttt{lambda-api-role} &
  \code{dynamodb:GetItem}\newline \code{dynamodb:Query}\newline \code{dynamodb:Scan} &
  L'API ne fait que lire --- aucune permission d'écriture accordée. \\
\hline
\texttt{ingestion-role} &
  \code{s3:PutObject}\newline \code{dynamodb:PutItem}\newline \code{dynamodb:BatchWriteItem} &
  L'ingestion écrit S3 et DynamoDB, sans relecture. \\
\hline
\texttt{aggregation-role} &
  \code{dynamodb:Query} (Silver)\newline \code{dynamodb:PutItem} (Gold) &
  Isolation stricte : lecture Silver, écriture Gold. \\
\hline
\end{tabularx}\end{center}

Cette séparation garantit qu'une compromission d'un Lambda d'agrégation ne peut ni
écrire dans les tables Silver ni lire d'autres tables Gold que les siennes. En
production, les rôles seraient affinés par préfixe de table via des conditions
\texttt{IAM StringLike} sur l'ARN.

\subsubsection{Chiffrement des données}

Le chiffrement en transit est assuré par défaut en HTTPS (TLS 1.2+) sur tous les
appels boto3. Le chiffrement au repos est natif sur chaque service managé.

\needspace{7\baselineskip}
\begin{center}\begin{tabularx}{\linewidth}{|l|l|>{\raggedright\arraybackslash}X|}
\hline
\rowcolor{tableHeader}
\thead{Service} & \thead{Mode de chiffrement} & \thead{Implémentation projet} \\
\hline
AWS S3 & SSE-S3 (clé gérée AWS) & Chiffrement côté serveur automatique \\
\hline
AWS DynamoDB & AES-256 (clé AWS) & Activé par défaut sur toutes les tables \\
\hline
API Gateway & TLS 1.2+ obligatoire & Aucun trafic HTTP non chiffré \\
\hline
\end{tabularx}\end{center}

Pour des données sensibles (positions GPS, données personnelles de transport), le
SSE-KMS avec rotation annuelle de clé serait retenu : il permet de révoquer l'accès
en révoquant la clé, sans supprimer les objets.

\subsubsection{Automatisation de l'ingestion par EventBridge}

L'ingestion manuelle actuelle serait industrialisée par AWS EventBridge Scheduler :
un schedule cron déclenche chaque Lambda d'ingestion toutes les 5 minutes.

\begin{lstlisting}[language=bash, caption=EventBridge Scheduler -- déclenchement périodique (AWS CLI)]
aws scheduler create-schedule \
  --name "ingest-parkings-every-5min" \
  --schedule-expression "rate(5 minutes)" \
  --target '{
    "Arn": "arn:aws:lambda:eu-west-1:ACCOUNT:function:ingest-parkings",
    "RoleArn": "arn:aws:iam::ACCOUNT:role/scheduler-invoke-role"
  }' \
  --flexible-time-window '{"Mode": "OFF"}'
\end{lstlisting}

Le rôle \texttt{scheduler-invoke-role} ne dispose que de la permission
\texttt{lambda:InvokeFunction} sur la fonction ciblée --- nouvelle application du
moindre privilège.

\subsection{Optimisation des performances et des coûts}

\subsubsection{Serverless contre serveur permanent}

L'architecture Lambda + API Gateway est coût-efficace pour un flux intermittent. La
comparaison avec une instance EC2 permanente illustre le gain.

\needspace{8\baselineskip}
\begin{center}\begin{tabularx}{\linewidth}{|l|>{\raggedright\arraybackslash}X|>{\raggedright\arraybackslash}X|}
\hline
\rowcolor{tableHeader}
\thead{Critère} & \thead{Lambda + API Gateway} & \thead{EC2 t3.small} \\
\hline
Facturation & À la requête (0,20 \$/M appels) & À l'heure ($\approx$18 \$/mois) \\
\hline
Scaling & Automatique, instantané & Manuel ou auto-scaling (délai) \\
\hline
Coût en veille & 0 \$ & 18 \$/mois (actif 24/7) \\
\hline
Maintenance OS & Aucune (managé) & Patches, mises à jour, supervision \\
\hline
\end{tabularx}\end{center}

Pour le profil du projet (appels toutes les 5 minutes), la facturation Lambda est
négligeable : $\approx$ 8\,640 invocations/mois, très en dessous du palier gratuit
AWS (1 million d'invocations/mois).

\subsubsection{Cycle de vie du stockage S3}

Le bucket accumule des snapshots toutes les 5 minutes ; sans politique de rétention,
le coût de stockage croît indéfiniment. Une politique de cycle de vie S3 automatise
l'archivage puis la suppression.

\needspace{7\baselineskip}
\begin{center}\begin{tabularx}{\linewidth}{|>{\raggedright\arraybackslash}X|l|l|}
\hline
\rowcolor{tableHeader}
\thead{Règle} & \thead{Délai} & \thead{Effet} \\
\hline
Transition vers S3 Infrequent Access & 30 jours & $-40\,\%$ de coût \\
\hline
Transition vers S3 Glacier & 90 jours & $-70\,\%$ (récupération lente) \\
\hline
Suppression automatique & 365 jours & Coût nul \\
\hline
\end{tabularx}\end{center}

\subsubsection{Supervision CloudWatch}

CloudWatch collecte automatiquement les métriques de chaque service. Une alarme sur
\texttt{ThrottledRequests} $>$ 0 déclenche une notification SNS, permettant de réagir
avant que le throttling n'affecte les utilisateurs.

\needspace{9\baselineskip}
\begin{center}\begin{tabularx}{\linewidth}{|l|l|>{\raggedright\arraybackslash}X|}
\hline
\rowcolor{tableHeader}
\thead{Service} & \thead{Métrique} & \thead{Seuil d'alerte} \\
\hline
Lambda ingestion & \texttt{Errors} & $>$ 0 erreur en 5 min \\
\hline
Lambda API & \texttt{Duration} & $>$ 3 secondes \\
\hline
DynamoDB & \texttt{ThrottledRequests} & $>$ 0 (critique) \\
\hline
API Gateway & \texttt{5XXError} & $>$ 1\,\% du trafic \\
\hline
S3 & \texttt{NumberOfObjects} & Croissance anormale \\
\hline
\end{tabularx}\end{center}

\capture{Tableau de bord Streamlit -- page Parkings : taux d'occupation global en temps réel et carte des parkings disponibles.}

\subsection{Synthèse --- compétences démontrées}

Le \texttt{projet cloud} couvre l'intégralité du périmètre du Bloc 3 à travers une
architecture serverless AWS complète, du data lake brutes à l'API de
consommation.

\subsubsection{Preuves d'acquisition des compétences}
\label{sec:preuves-bloc3}

Le tableau suivant récapitule, pour chacune des quatre sous-compétences du Bloc 3, la
réalisation concrète qui la démontre dans le projet.

\needspace{12\baselineskip}
\begin{center}
\begin{tabularx}{\linewidth}{|>{\raggedright\arraybackslash}p{4.4cm}|>{\raggedright\arraybackslash}X|}
\hline
\rowcolor{tableHeader}
\thead{Compétence visée} & \thead{Réalisation et preuve dans le projet} \\
\hline
Stockage de données dans le cloud &
  Lac de données S3 horodaté (Bronze) et base DynamoDB managée (Silver/Gold),
  avec choix de services justifié face aux alternatives AWS. \\
\hline
Pipelines de traitement cloud &
  Ingestion incrémentale idempotente S3 $\rightarrow$ DynamoDB et pipeline
  d'agrégation produisant 10 tables analytiques pré-calculées. \\
\hline
Sécurité (chiffrement \& IAM) &
  Rôles IAM au moindre privilège, chiffrement au repos (SSE-S3, AES-256) et en
  transit (TLS 1.2+), automatisation sécurisée via EventBridge. \\
\hline
Optimisation (performance, disponibilité, coûts) &
  GSI pour un accès O(1), compute serverless facturé à l'usage, cycle de vie S3 et
  supervision CloudWatch (métriques + alarmes SNS). \\
\hline
\end{tabularx}
\end{center}

\newpage
\section[Bloc 4 -- Implementer des methodes d'intelligence artificielle]{Bloc 4 -- Implementer des methodes d'intelligence artificielle pour\\ modeliser et predire de nouveaux comportements et usages}

Cette partie démontre l'acquisition des cinq compétences du Bloc 4 du référentiel
RNCP36739~\cite{rncp} à travers un projet complet de \textbf{maintenance prédictive
    industrielle}. Chaque étape couvre une ou plusieurs sous-compétences du référentiel,
synthétisées dans le tableau de validation ci-dessous.

% ===================== COMPÉTENCES VISÉES ==================
\subsection{Compétences visées}

Le référentiel RNCP36739 définit cinq sous-compétences pour le Bloc 4, que ce
dossier couvre intégralement à travers le \texttt{data science project}.
Ce bloc porte sur l'exploitation des données pour produire des modèles prédictifs
à valeur métier, du reporting analytique et de la visualisation. Il couvre le cycle
complet du Machine Learning : préparation des données, construction de pipelines de
transformation, entraînement comparatif de modèles, évaluation rigoureuse et mise
en service. Le tableau de preuves d'acquisition présenté en fin de dossier
(section~\ref{sec:preuves}) récapitule, pour chacune, la réalisation concrète qui
la démontre.

\begin{itemize}[leftmargin=1.4em, itemsep=3pt, topsep=3pt]
    \item Extraire des données en provenance de systèmes d'information variés pour
          les exploiter et les analyser en utilisant les outils professionnels courants.
    \item Préparer les données en les transformant et en les nettoyant pour faire
          l'analyse et le reporting selon les besoins des différents métiers.
    \item Élaborer une communication infographique visuelle en construisant des
          tableaux de bord interactifs afin de communiquer les résultats d'analyse et
          d'assurer l'extraction de connaissances en temps réel.
    \item Développer un modèle prédictif pour identifier de nouveaux comportements
          et usages en implémentant des algorithmes d'apprentissage automatique supervisés.
    \item Évaluer la performance d'un modèle de machine learning en analysant ses
          résultats et en les comparant avec d'autres modèles afin d'implémenter la
          solution la plus convenable à un cas d'usage.
\end{itemize}

% ===================== CONTEXTE ============================
\subsection{Contexte et enjeux}

Le projet \texttt{data science} traite de la \textbf{maintenance
    prédictive industrielle}. Les machines industrielles sont équipées de capteurs
(vibrations, température moteur, vitesse de rotation, pression, heures de vie
résiduelle) qui produisent un flux continu de mesures. À partir de ces signaux,
nous traitons deux tâches de prédiction au moyen de scikit-learn~\cite{sklearn} :

\begin{itemize}[leftmargin=1.4em, itemsep=2pt, topsep=3pt]
    \item \textbf{Classification binaire} --- prédire si une panne surviendra dans
          les prochaines 24 heures. Cette prédiction déclenche une intervention préventive
          ciblée avant la défaillance.
    \item \textbf{Classification multiclasse (5 classes)} --- identifier le type de
          panne attendu. Cette prédiction oriente le technicien vers la pièce ou le
          sous-système en cause, réduisant le temps de diagnostic.
\end{itemize}

L'enjeu industriel est direct et chiffrable. Une fausse alerte mobilise une équipe
de maintenance pour rien ; un faux négatif laisse passer une panne réelle qui peut
coûter plusieurs heures d'arrêt de production. Cet arbitrage asymétrique justifie
le choix de la métrique primaire : le \textbf{F1-score} (moyenne harmonique de la
précision et du rappel), qui pénalise simultanément les fausses alertes et les
pannes manquées.

% ===================== ARCHITECTURE / CYCLE ML =============
\subsection{Architecture et cycle de vie du modèle}

L'architecture du projet suit le cycle de vie standard d'un projet ML, de la donnée
brute jusqu'à la mise en service. Chaque étape est isolée dans un module dédié, ce
qui garantit la testabilité et la reproductibilité de l'ensemble.

\needspace{20\baselineskip}
\begin{center}\fitwidth{%
        \begin{tikzpicture}[node distance=0.8cm]

            \node[flowbox, fill=black!5, draw=black!30, text width=11cm] (csv) {
                \textbf{Données brutes} --- \texttt{industrial\_machine\_maintenance.csv}\\[2pt]
                \small vibration\_rms · temperature\_motor · rpm · pressure\_level · rul\_hours · operating\_mode
            };

            \node[flowbox, fill=silverBg, draw=silverColor, below=0.7cm of csv, text width=11cm] (prep) {
                \textbf{\textcolor{silverColor}{Préparation}} \quad \texttt{preprocessing.py}\\[2pt]
                \small ColumnTransformer (imputation + normalisation + encodage) · split stratifié 80/20
            };

            \node[flowbox, fill=cloudBg, draw=cloudColor, below=0.7cm of prep, text width=11cm] (train) {
                \textbf{\textcolor{cloudColor}{Entraînement comparatif}} \quad \texttt{train.py}\\[2pt]
                \small 4 modèles candidats par tâche · sélection automatique du meilleur · permutation importance
            };

            \node[flowbox, fill=mlBg, draw=mlColor, below=0.7cm of train, text width=11cm] (art) {
                \textbf{\textcolor{mlColor}{Évaluation \& artefacts}} \quad \texttt{evaluate.py}\\[2pt]
                \small \texttt{best\_model.joblib} · \texttt{model\_comparison.csv} · \texttt{feature\_importance.csv}
            };

            \node[minibox, fill=goldBg, draw=goldColor, below left=0.9cm and 1.5cm of art, text width=4.6cm] (api) {
                \textbf{\textcolor{goldColor!70!black}{Serving --- FastAPI}}\\[2pt]
                \small \texttt{api/routes.py}\\ predict + health
            };
            \node[minibox, fill=goldBg, draw=goldColor, below right=0.9cm and 1.5cm of art, text width=4.6cm] (dash) {
                \textbf{\textcolor{goldColor!70!black}{Dashboard --- Streamlit}}\\[2pt]
                \small \texttt{dashboard/app.py}\\ EDA · benchmark · prédiction
            };

            \draw[arrowM] (csv) -- (prep);
            \draw[arrowM] (prep) -- (train);
            \draw[arrowM] (train) -- (art);
            \draw[arrowM] (art.south) -- ++(0,-0.4) -| (api.north);
            \draw[arrowM] (art.south) -- ++(0,-0.4) -| (dash.north);

        \end{tikzpicture}}\end{center}

Ce découpage modulaire est un choix d'ingénierie : la préparation, l'entraînement,
l'évaluation et le serving sont indépendants. On peut réentraîner les modèles sans
toucher à l'API, ou faire évoluer le tableau de bord sans réentraîner. Il est
impératif de séparer les responsabilités pour que le projet ML puisse être maintenu.

% ===================== ANALYSE DU DOMAINE ==================
\subsection{Analyse exploratoire et compréhension métier}

La maintenance industrielle suit historiquement deux modèles. La maintenance
\textit{corrective} répare après la panne : elle maximise l'usure utile des pièces
mais expose à des arrêts non planifiés et coûteux. La maintenance \textit{préventive
    planifiée} révise à intervalle fixe : elle est sûre mais remplace des pièces encore
fonctionnelles, gaspillant de la valeur.

La maintenance \textit{prédictive} vise un troisième modèle, supérieur aux deux
précédents : intervenir au bon moment, ni trop tôt ni trop tard, en se fondant sur
l'état réel de la machine mesuré par les capteurs. C'est précisément l'objet des
deux tâches de prédiction du projet.

\needspace{8\baselineskip}
\begin{center}
    \begin{tabularx}{\linewidth}{|l|l|>{\raggedright\arraybackslash}X|>{\raggedright\arraybackslash}X|}
        \hline
        \rowcolor{tableHeader}
        \thead{Stratégie}    & \thead{Pré-requis} & \thead{Coût d'une panne} & \thead{Coût de maintenance}      \\
        \hline
        Corrective           & Aucun              & Élevé (arrêt non prévu)  & Faible (minimal)                 \\
        \hline
        Préventive planifiée & Calendrier         & Faible                   & Élevé (pièces saines remplacées) \\
        \hline
        \textbf{Prédictive}  & Capteurs + ML      & Quasi-nul                & Optimisé (juste à temps)         \\
        \hline
    \end{tabularx}
\end{center}

L'indicateur \texttt{rul\_hours} (\textit{Remaining Useful Life}, durée de vie
résiduelle) est au cœur du problème : il estime le temps restant avant la prochaine
défaillance. Comme le confirmera l'analyse d'importance des variables, cet indicateur
domine la prédiction --- un résultat cohérent avec l'intuition métier.

\subsubsection{Exploration des données}

Le jeu de données  regroupe 24\,042 mesures de capteurs. L'analyse exploratoire
(EDA) précède la modélisation : elle révèle le fort déséquilibre des cibles
et la séparation des distributions decapteurs selon le statut de panne, deux
observations qui orientent les choix méthodologiques.

\figc{fig_distribution_cibles.png}{0.95\linewidth}{Distribution des deux cibles. La
    classe positive \texttt{failure\_within\_24h=1} ne représente que 14,8\,\% des
    observations, et la cible \texttt{failure\_type} est dominée par la classe
    \texttt{none} (85\,\%). Ce déséquilibre impose le F1-score et la PR-AUC comme
    métriques, ainsi qu'un split stratifié.}

Les boîtes à moustaches confirment que les variables dominantes séparent nettement
les machines saines des machines à risque : sur les machines en panne, la température
moyenne passe de 49 à 63\,°C et la durée de vie résiduelle chute de 30,6 à 11,8\,h.

\figc{fig_boxplots.png}{0.95\linewidth}{Distribution des variables clés selon le
    statut de panne. La séparation nette des médianes (température, vibration, RUL)
    justifie leur pouvoir prédictif observé plus loin.}

\figc{fig_heatmap.png}{0.62\linewidth}{Carte de chaleur des corrélations. La
    température moteur ($+0{,}39$), la vibration ($+0{,}26$) et la durée de vie
    résiduelle ($-0{,}25$) sont les plus corrélées à la cible binaire.}

% ===================== PRÉPARATION =========================
\subsection{Préparation des données et pipeline de transformation}

\subsubsection{Variables explicatives et cibles}

Le jeu de données contient six variables explicatives (cinq numériques, une catégorielle)
et deux cibles alternatives, une par tâche de prédiction.

\needspace{10\baselineskip}
\begin{center}
    \begin{tabularx}{\linewidth}{|l|l|>{\raggedright\arraybackslash}X|}
        \hline
        \rowcolor{tableHeader}
        \thead{Variable}              & \thead{Type}      & \thead{Description}                      \\
        \hline
        \texttt{rul\_hours}           & Numérique         & Heures de vie résiduelle estimée         \\
        \hline
        \texttt{temperature\_motor}   & Numérique         & Température moteur (°C)                  \\
        \hline
        \texttt{rpm}                  & Numérique         & Vitesse de rotation (tours/min)          \\
        \hline
        \texttt{vibration\_rms}       & Numérique         & Amplitude vibratoire RMS                 \\
        \hline
        \texttt{pressure\_level}      & Numérique         & Niveau de pression                       \\
        \hline
        \texttt{operating\_mode}      & Catégoriel        & Mode de fonctionnement                   \\
        \hline\hline
        \texttt{failure\_within\_24h} & Cible binaire     & Une panne surviendra-t-elle sous 24\,h ? \\
        \hline
        \texttt{failure\_type}        & Cible multiclasse & Type de panne (5 classes)                \\
        \hline
    \end{tabularx}
\end{center}

\subsubsection{Le pipeline de transformation : ColumnTransformer}

Les variables numériques et la variable catégorielle exigent des traitements
distincts. Le \texttt{ColumnTransformer} de scikit-learn applique deux sous-pipelines
en parallèle, puis concatène leurs sorties en une matrice de features homogène
prête pour l'estimateur. Le schéma ci-dessous illustre ce flux.

\needspace{24\baselineskip}
\begin{center}\fitwidth{%
        \begin{tikzpicture}[node distance=1cm]

            % Entrée
            \node[flowbox, fill=black!5, draw=black!30, text width=10.5cm] (input) {
                \textbf{Features en entrée} --- 6 colonnes\\[2pt]
                \small vibration\_rms · temperature\_motor · rpm · pressure\_level · rul\_hours · operating\_mode
            };

            % Deux branches
            \node[minibox, fill=silverBg, draw=silverColor, below=1.6cm of input, xshift=-3.3cm, text width=5.5cm] (num) {
                \textbf{\textcolor{silverColor}{Branche numérique}} --- 5 colonnes\\[3pt]
                \small \texttt{SimpleImputer(median)}\\[1pt] + \texttt{StandardScaler}
            };
            \node[minibox, fill=goldBg, draw=goldColor, below=1.6cm of input, xshift=3.3cm, text width=5.5cm] (cat) {
                \textbf{\textcolor{goldColor!70!black}{Branche catégorielle}} --- 1 colonne\\[3pt]
                \small \texttt{SimpleImputer(most\_frequent)}\\[1pt] + \texttt{OneHotEncoder(ignore)}
            };

            % Conteneur ColumnTransformer
            \node[draw=accentBlue, very thick, rounded corners, fit=(num)(cat), inner sep=12pt,
                label={[text=accentBlue, font=\bfseries]north:ColumnTransformer}] (ct) {};

            % Fusion
            \node[flowbox, fill=cloudBg, draw=cloudColor, below=1.5cm of ct, text width=9cm] (trans) {
                \textbf{\textcolor{cloudColor}{Matrice de features transformée}}\\[2pt]
                \small vecteur numérique homogène (numériques standardisées + catégorielles encodées)
            };

            \node[minibox, fill=mlBg, draw=mlColor, below=0.9cm of trans, text width=6cm] (est) {
                \textbf{\textcolor{mlColor}{Estimateur}}\\[2pt]
                \small ex. \texttt{HistGradientBoostingClassifier}
            };

            \node[below=0.7cm of est, font=\ttfamily\small, text=titleNavy] (pred) {predict() \quad·\quad predict\_proba()};

            % Flèches
            \draw[arrowM] (input.south) -- ++(0,-0.45) -| (num.north);
            \draw[arrowM] (input.south) -- ++(0,-0.45) -| (cat.north);
            \draw[arrowM] (num.south) -- ++(0,-0.5) -| ([xshift=-2.2cm]trans.north);
            \draw[arrowM] (cat.south) -- ++(0,-0.5) -| ([xshift=2.2cm]trans.north);
            \draw[arrowM] (trans) -- (est);
            \draw[arrowM] (est) -- (pred);

        \end{tikzpicture}}\end{center}

\begin{lstlisting}[language=Python, caption=Pipeline de préparation -- ColumnTransformer et split stratifié (preprocessing.py)]
def build_preprocessor() -> ColumnTransformer:
    numeric_pipeline = Pipeline([
        ("imputer", SimpleImputer(strategy="median")),
        ("scaler",  StandardScaler()),
    ])
    categorical_pipeline = Pipeline([
        ("imputer", SimpleImputer(strategy="most_frequent")),
        ("encoder", OneHotEncoder(handle_unknown="ignore")),
    ])
    return ColumnTransformer(transformers=[
        ("numeric",     numeric_pipeline,     NUMERIC_COLUMNS),
        ("categorical", categorical_pipeline, CATEGORICAL_COLUMNS),
    ])

def make_train_test_split(features, target, test_size=0.2):
    validate_stratified_split_capacity(target, test_size)
    return train_test_split(
        features, target,
        test_size=test_size, random_state=RANDOM_STATE,
        stratify=target,   # garantit la representation de toutes les classes
    )
\end{lstlisting}

Chaque choix de transformation répond à une contrainte du domaine. L'imputation par
la \textbf{médiane} pour les variables numériques est robuste aux valeurs aberrantes
fréquentes sur des capteurs. L'option \texttt{handle\_unknown="ignore"} de
l'encodeur prévient toute erreur à l'inférence si un mode opératoire inconnu apparaît
en production : la ligne est encodée par un vecteur nul plutôt que de provoquer une
exception. Enfin, la \textbf{stratification} du split garantit que chaque classe ---
y compris les pannes rares --- est représentée dans le jeu de test avec la même
proportion qu'à l'entraînement, condition d'une évaluation statistiquement valide.

La taille du jeu de test est par ailleurs calculée dynamiquement par
\texttt{test\_size = max(0.2,\,n\_classes / n\_rows)}. Ce garde-fou empêche qu'une
division 80/20 sur un jeu restreint produise un jeu de test trop petit pour contenir
au moins un exemple de chaque classe.

% ===================== ENTRAÎNEMENT ========================
\subsection{Entraînement comparatif et sélection du modèle}

\subsubsection{Architecture du benchmark}

Le principe directeur est l'\textbf{étanchéité} entre l'entraînement et l'évaluation.
Le préprocesseur et le modèle sont encapsulés dans un unique \texttt{Pipeline}
scikit-learn : le \texttt{StandardScaler} et les imputeurs sont ajustés uniquement
sur les données d'entraînement, jamais sur le jeu de test. Cette discipline élimine
toute fuite de données (\textit{data leakage}) qui gonflerait artificiellement les
scores.

\begin{lstlisting}[language=Python, caption=Boucle de benchmark et sélection automatique (train.py)]
for model_name, estimator in candidate_models.items():
    pipeline = Pipeline(steps=[
        ("preprocessor", build_preprocessor()),
        ("model",        estimator),
    ])
    pipeline.fit(x_train, y_train)
    predicted_labels        = pipeline.predict(x_test)
    predicted_probabilities = pipeline.predict_proba(x_test)

    evaluation_rows.append(evaluate_classifier(
        task_name=task_name, model_name=model_name,
        true_values=y_test,
        predicted_labels=predicted_labels,
        predicted_probabilities=predicted_probabilities,
    ))

comparison    = compare_results(task_name=task_name, results=evaluation_rows)
best_row      = select_best_model(comparison)   # tri sur la metrique primaire
best_pipeline = trained_pipelines[best_row["model_name"]]
save_model(best_pipeline, model_path)           # serialisation joblib
\end{lstlisting}

La sélection du meilleur modèle est entièrement automatisée : les résultats sont
triés sur la métrique primaire (F1 en binaire, weighted\_F1 en multiclasse) et le
vainqueur est sérialisé en \texttt{joblib} pour la mise en service. Aucune
intervention manuelle n'intervient dans le choix final, ce qui rend la démarche
reproductible.

\subsubsection{Modèles candidats}

Quatre familles de modèles sont mises en concurrence pour chaque tâche, couvrant un
spectre volontairement large : un modèle linéaire (Logistic Regression), deux
modèles d'ensemble (Random Forest, gradient boosting) et un réseau de neurones
(Multilayer Perceptron).

\begin{lstlisting}[language=Python, caption=Modèles candidats par tâche (models.py)]
# Tache binaire (failure_within_24h)
{
  "logistic_regression": LogisticRegression(max_iter=1000, random_state=42),
  "random_forest":       RandomForestClassifier(
                             n_estimators=300, random_state=42,
                             class_weight="balanced"),  # compense le desequilibre
  "gradient_boosting":   HistGradientBoostingClassifier(random_state=42),
  "mlp_classifier":      MLPClassifier(
                             hidden_layer_sizes=(64, 32),
                             max_iter=300, random_state=42),
}
\end{lstlisting}

L'option \texttt{class\_weight="balanced"} de la Random Forest compense le
déséquilibre de classes inhérent aux données de maintenance --- les pannes sont
rares par définition. Le \texttt{HistGradientBoostingClassifier} gère nativement les
valeurs manquantes, sans imputation préalable, ce qui en fait un candidat robuste
même lorsqu'un capteur perd des mesures.

\subsubsection{Le modèle vainqueur : HistGradientBoostingClassifier}

Le \texttt{HistGradientBoostingClassifier} est l'implémentation scikit-learn du
gradient boosting (approche popularisée par LightGBM). Il se
distingue par deux optimisations déterminantes :

\begin{enumerate}[leftmargin=1.6em, itemsep=3pt, topsep=3pt]
    \item \textbf{Discrétisation par histogrammes.} Plutôt que de tester tous les
          seuils de division possibles, l'algorithme regroupe chaque variable en 256
          intervalles (bins) et ne teste que ces 256 valeurs. La complexité d'entraînement
          chute de $O(n \times d)$ à $O(256 \times d)$, où $n$ est le nombre d'exemples et
          $d$ le nombre de variables.
    \item \textbf{Gestion native des valeurs manquantes.} L'algorithme apprend, pour
          chaque division, si une valeur manquante doit suivre la branche gauche ou droite.
          Cette propriété est directement utile face à des capteurs pouvant perdre des
          mesures (panne capteur, coupure réseau).
\end{enumerate}

\needspace{8\baselineskip}
\begin{center}
    \begin{tabularx}{\linewidth}{|l|>{\raggedright\arraybackslash}X|>{\raggedright\arraybackslash}X|}
        \hline
        \rowcolor{tableHeader}
        \thead{Caractéristique}    & \thead{HistGradientBoosting}           & \thead{RandomForest}               \\
        \hline
        Valeurs manquantes         & Nativement gérées                      & Imputation requise                 \\
        \hline
        Complexité d'entraînement  & $O(256 \times d \times \text{arbres})$ & $O(n \log n \times \text{arbres})$ \\
        \hline
        Risque de surapprentissage & Modéré (régularisation $\ell_2$)       & Faible (bagging)                   \\
        \hline
        Interprétabilité           & Faible (boîte noire)                   & Moyenne (importance native)        \\
        \hline
        Gestion du déséquilibre    & Bonne (\texttt{class\_weight})         & Très bonne (\texttt{balanced})     \\
        \hline
    \end{tabularx}
\end{center}

Comme le montreront les résultats, aucun algorithme n'est universellement supérieur :
le Gradient Boosting l'emporte sur la tâche binaire, tandis que la Random Forest
domine la tâche multiclasse déséquilibrée. Le benchmark systématique est donc
indispensable pour choisir le bon modèle par cas d'usage.

% ===================== RÉSULTATS ===========================
\subsection{Résultats et évaluation}

\subsubsection{Tâche 1 --- Classification binaire \texttt{failure\_within\_24h}}

\needspace{10\baselineskip}
\begin{center}
    \begin{tabularx}{\linewidth}{|>{\raggedright\arraybackslash}X|c|c|c|c|c|}
        \hline
        \rowcolor{tableHeader}
        \thead{Modèle}             & \thead{F1}      & \thead{Précision} & \thead{Rappel} & \thead{ROC-AUC} & \thead{PR-AUC}  \\
        \hline
        \textbf{gradient boosting} & \textbf{0,9775} & 0,9789            & 0,9761         & \textbf{0,9991} & \textbf{0,9956} \\
        \hline
        random forest              & 0,9752          & 0,9857            & 0,9649         & 0,9995          & 0,9976          \\
        \hline
        mlp classifier             & 0,9337          & 0,9697            & 0,9003         & 0,9974          & 0,9877          \\
        \hline
        logistic regression        & 0,7224          & 0,8456            & 0,6306         & 0,9485          & 0,8273          \\
        \hline
    \end{tabularx}
\end{center}

Le \texttt{Gradient Boosting} remporte le benchmark sur la métrique
primaire F1 (0,9775), avec le meilleur équilibre précision/rappel. La Random Forest
obtient une précision et une ROC-AUC légèrement supérieures, mais son rappel plus
faible (0,9649) --- donc davantage de pannes manquées --- pénalise son F1. La
Logistic Regression confirme la nature non linéaire du problème : son rappel de
0,6306 signifie qu'elle manque près de 37\,\% des pannes réelles, ce qui la rend
inutilisable pour ce cas d'usage critique.

\figc{fig_comparaison_binaire.png}{0.95\linewidth}{Comparaison visuelle des quatre
    modèles sur la tâche binaire (F1, précision, rappel, ROC-AUC, PR-AUC). Les deux
    modèles d'ensemble dominent ; la régression logistique décroche nettement sur le
    rappel et la PR-AUC.}

\subsubsection{Tâche 2 --- Classification multiclasse \texttt{failure\_type} (5 classes)}

\needspace{8\baselineskip}
\begin{center}
    \begin{tabularx}{\linewidth}{|>{\raggedright\arraybackslash}X|c|c|c|}
        \hline
        \rowcolor{tableHeader}
        \thead{Modèle}           & \thead{weighted\_F1} & \thead{macro\_F1} & \thead{Classes} \\
        \hline
        \textbf{random\_forest}  & \textbf{0,9782}      & 0,8955            & 5               \\
        \hline
        hist\_gradient\_boosting & 0,9753               & 0,8829            & 5               \\
        \hline
    \end{tabularx}
\end{center}

Sur la tâche multiclasse, la Random Forest prend la tête (weighted\_F1 = 0,9782).
Le macro\_F1 de 0,8955 --- moyenne non pondérée par la taille des classes --- indique
que les classes minoritaires restent globalement bien gérées. L'écart entre
weighted\_F1 (0,9782) et macro\_F1 (0,8955) révèle néanmoins que certaines classes
rares sont prédites avec moins de fiabilité : c'est l'axe d'amélioration principal,
détaillé en section~\ref{sec:axes}.

\subsubsection{Interprétabilité --- importance des variables}

L'interprétabilité est essentielle pour qu'un modèle soit accepté en production par
les équipes métier. Nous calculons l'\textbf{importance par permutation} sur le jeu de
test : pour chaque variable, on mesure la chute du F1 lorsque ses valeurs sont
mélangées aléatoirement. Plus la chute est forte, plus la variable est prédictive.

\needspace{9\baselineskip}
\begin{center}
    \begin{tabularx}{\linewidth}{|>{\raggedright\arraybackslash}X|c|}
        \hline
        \rowcolor{tableHeader}
        \thead{Variable}            & \thead{Importance (permutation)} \\
        \hline
        \texttt{rul\_hours}         & \textbf{0,4954}                  \\
        \hline
        \texttt{temperature\_motor} & 0,2899                           \\
        \hline
        \texttt{rpm}                & 0,2772                           \\
        \hline
        \texttt{vibration\_rms}     & 0,1165                           \\
        \hline
        \texttt{pressure\_level}    & 0,0471                           \\
        \hline
        \texttt{operating\_mode}    & 0,0174                           \\
        \hline
    \end{tabularx}
\end{center}

La variable \texttt{rul\_hours} domine très nettement (0,495) : elle est le signal le
plus prédictif d'une panne imminente, ce qui valide l'intuition métier. La
température moteur (0,290) et la vitesse de rotation (0,277) sont les deux facteurs
physiques complémentaires. À l'inverse, le mode opératoire est quasi nul (0,017) : il
n'apporte pas d'information prédictive significative sur ce jeu de données. Ce
classement est directement exploitable : il indique aux équipes quelles grandeurs
instrumenter en priorité.

\begin{lstlisting}[language=Python, caption=Calcul de L'importance par permutation (train.py)]
importance_result = permutation_importance(
    estimator=best_pipeline,
    X=x_test, y=y_test,
    n_repeats=10,           # robustesse statistique de l'estimation
    random_state=RANDOM_STATE,
    scoring="f1_weighted",
)
importance_frame = pd.DataFrame({
    "feature":         x_test.columns,
    "importance_mean": importance_result.importances_mean,
    "importance_std":  importance_result.importances_std,
}).sort_values(by="importance_mean", ascending=False)
\end{lstlisting}

\figc{fig_importance_binaire.png}{0.85\linewidth}{Importance des variables par
    permutation (tâche binaire). La durée de vie résiduelle \texttt{rul\_hours} explique
    à elle seule près de la moitié de la performance du modèle.}

\subsubsection{Analyse des erreurs --- matrice de confusion}

Au-delà des scores agrégés, l'analyse de la matrice de confusion
éclaire le comportement réel du modèle face aux deux types d'erreurs.

\needspace{8\baselineskip}
\begin{center}
    \begin{tabular}{|l|>{\centering\arraybackslash}p{3.9cm}|>{\centering\arraybackslash}p{3.9cm}|}
        \hline
        \rowcolor{tableHeader}
        \thead{}                     & \thead{Prédit : panne}                   & \thead{Prédit : pas de panne}        \\
        \hline
        \textbf{Réel : panne}        & VP (vrais positifs)                      & FN (faux négatifs --- risque métier) \\
        \hline
        \textbf{Réel : pas de panne} & FP (faux positifs --- coût mobilisation) & VN (vrais négatifs)                  \\
        \hline
    \end{tabular}
\end{center}

En maintenance industrielle, les deux erreurs n'ont pas le même coût : un faux négatif
(panne manquée) peut entraîner un arrêt de production, tandis qu'un faux positif
(fausse alerte) ne mobilise qu'une équipe pour une vérification inutile. Le modèle
retenu affiche un rappel de 0,9761 (97,6\,\% des pannes réelles détectées) pour une
précision de 0,9789 : l'équilibre penche légèrement en faveur du rappel, ce qui est
exactement le comportement souhaité pour ce cas d'usage.

\figc{fig_confusion_binaire.png}{0.62\linewidth}{Matrice de confusion de la tâche
    binaire (modèle \texttt{gradient\_boosting}). Sur le jeu de test, seuls 17 faux
    négatifs et 15 faux positifs subsistent face à 4\,082 vrais négatifs et 695 vrais
    positifs.}

Sur la tâche multiclasse, la matrice de confusion révèle que la classe majoritaire
\texttt{none} est presque parfaitement identifiée, tandis que les confusions
résiduelles se concentrent entre types de pannes rares (\texttt{electrical},
\texttt{hydraulic}) --- ce qui explique l'écart entre weighted\_F1 et macro\_F1.

\figc{fig_confusion_multiclasse.png}{0.7\linewidth}{Matrice de confusion de la tâche
    multiclasse (modèle \texttt{random\_forest}, 5 classes). La diagonale dominante
    confirme la qualité globale ; les erreurs se concentrent sur les classes minoritaires.}

% ===================== MISE EN SERVICE =====================
\subsection{Mise en service et communication des résultats}

\subsubsection{API de prédiction --- FastAPI}

Les modèles sérialisés sont exposés via une API REST construite avec FastAPI~\cite{fastapi}.
Chaque tâche dispose de son endpoint de prédiction, et un endpoint de supervision
expose l'état de chargement des artefacts.

\begin{lstlisting}[language=Python, caption=Endpoints de prédiction et supervision (api/routes.py)]
@router.post("/predict/failure-within-24h",
             response_model=FailureWithin24hPredictionResponse)
def predict_binary(payload: FailureWithin24hPredictionRequest):
    return predict_failure_within_24h(payload.model_dump())

@router.post("/predict/failure-type",
             response_model=FailureTypePredictionResponse)
def predict_multiclass(payload: FailureTypePredictionRequest):
    return predict_failure_type(payload.model_dump())

@router.get("/health", response_model=HealthResponse)
def health() -> HealthResponse:
    available_tasks = []
    for task_name in config.TASK_CONFIGS:
        try:
            load_task_bundle(task_name)
        except TaskArtifactError:
            continue
        available_tasks.append(task_name)
    status = "ok" if len(available_tasks) == len(config.TASK_CONFIGS) else "degraded"
    return HealthResponse(status=status, available_tasks=available_tasks)
\end{lstlisting}

L'endpoint \texttt{/health} renvoie \texttt{"ok"} uniquement si les deux artefacts
(binaire et multiclasse) sont chargés ; sinon \texttt{"degraded"}. Ce mécanisme
permet un monitoring en production. La validation des entrées est assurée par Pydantic v2
via les schémas \texttt{FailureWithin24hPredictionRequest} et
\texttt{FailureTypePredictionRequest}, cohérents avec les six variables déclarées
dans \texttt{config.py}.

\subsubsection{Tableau de bord interactif --- Streamlit}

Le tableau de bord Streamlit~\cite{streamlit} (\texttt{dashboard/app.py}) répond
directement à la sous-compétence de \og{}communication infographique visuelle\fg{}.
Il s'organise en trois pages complémentaires :

\begin{itemize}[leftmargin=1.4em, itemsep=2pt, topsep=3pt]
    \item \textbf{Page exploration (EDA)} : distributions des variables capteurs
          (histogrammes, boîtes à moustaches), corrélations entre variables (carte de
          chaleur), répartition des classes cibles. Elle permet à un expert métier de
          comprendre les données sans écrire de code.
    \item \textbf{Page benchmark} : tableau comparatif des modèles (F1, précision,
          rappel, ROC-AUC), graphique des importances de variables, sélection interactive
          du modèle visualisé.
    \item \textbf{Page prédiction} : formulaire de saisie des six variables capteurs,
          appel en temps réel à l'API FastAPI, affichage de la prédiction et de la
          probabilité associée.
\end{itemize}

La page de prédiction ne ré-embarque pas le modèle : elle consomme l'API. Ainsi,
tout déploiement d'un nouveau modèle est immédiatement reflété dans le tableau de
bord sans le redémarrer --- une conséquence directe de l'architecture découplée.

\figc{fig_dashboard.png}{\linewidth}{Tableau de bord Streamlit. À gauche :
    informations du modèle servi et comparaison des modèles. À droite : formulaire de
    saisie des six variables capteurs et réponse JSON de l'API (probabilité, label,
    top-3 des variables influentes).}

\subsubsection{Qualité logicielle et reproductibilité}

L'ensemble du pipeline est couvert par des tests automatisés \texttt{pytest}, module
par module, garantissant que chaque étape est validée indépendamment. Cette couverture
est la condition de la reproductibilité des résultats annoncés.

\needspace{9\baselineskip}
\begin{center}
    \begin{tabularx}{\linewidth}{|l|>{\raggedright\arraybackslash}X|}
        \hline
        \rowcolor{tableHeader}
        \thead{Module testé}                   & \thead{Périmètre de validation}                        \\
        \hline
        \texttt{test\_data.py}                 & Chargement du dataset, séparation features/cible       \\
        \hline
        \texttt{test\_preprocessing.py}        & Pipeline ColumnTransformer, split stratifié            \\
        \hline
        \texttt{test\_prediction\_pipeline.py} & Pipeline complet entraînement $\rightarrow$ prédiction \\
        \hline
        \texttt{test\_evaluate.py}             & Métriques binaires et multiclasses                     \\
        \hline
        \texttt{test\_explainability.py}       & Permutation importance                                 \\
        \hline
        \texttt{test\_api.py}                  & Endpoints FastAPI (health, predict)                    \\
        \hline
        \texttt{test\_dashboard\_helpers.py}   & Fonctions de visualisation Streamlit                   \\
        \hline
    \end{tabularx}
\end{center}

La configuration des tâches est par ailleurs centralisée dans un unique dictionnaire
\texttt{TASK\_CONFIGS} (\texttt{config.py}), source de vérité partagée par les modules
d'entraînement, d'évaluation et de serving. Ajouter une nouvelle tâche se résume à y
ajouter une entrée : aucun chemin n'est codé en dur dans les modules métier.

% ===================== ROBUSTESSE ==========================
\subsection{Robustesse, limites et axes d'amélioration}
\label{sec:axes}

\subsubsection{Validation : split stratifié et perspective k-fold}

La version actuelle repose sur un split stratifié 80/20, choisi pour son rapport
coût/robustesse : huit entraînements complets (quatre modèles $\times$ deux tâches)
restent rapides. Une validation croisée \textit{k-fold} (k=5) fournirait une
estimation plus robuste de la variance, au prix d'un temps de calcul multiplié par
cinq.

\needspace{8\baselineskip}
\begin{center}
    \begin{tabularx}{\linewidth}{|l|>{\raggedright\arraybackslash}X|>{\raggedright\arraybackslash}X|}
        \hline
        \rowcolor{tableHeader}
        \thead{Méthode}       & \thead{Avantage}                        & \thead{Inconvénient}               \\
        \hline
        Split 80/20 stratifié & Rapide, simple à implémenter            & Variance selon la graine aléatoire \\
        \hline
        k-fold (k=5)          & Estimation robuste (k résultats)        & 5$\times$ le temps de calcul       \\
        \hline
        Stratified k-fold     & Robustesse + représentation des classes & Même coût que k-fold               \\
        \hline
    \end{tabularx}
\end{center}

\subsubsection{Limites de l'interprétabilité par permutation}

L'importance par permutation reste une méthode globale qui présente deux limites. D'une
part, en présence de variables corrélées (par exemple \texttt{rul\_hours} et
\texttt{temperature\_motor}), permuter l'une laisse l'information accessible via
l'autre, sous-estimant l'importance des deux. D'autre part, son coût croît avec le
produit du nombre de variables par \texttt{n\_repeats}. Une approche complémentaire
serait SHAP (\textit{SHapley Additive exPlanations}), qui fournit une interprétabilité
\textbf{locale}, instance par instance, en plus de la vue globale.

\subsubsection{Axes d'amélioration}

L'écart entre macro\_F1 (0,8955) et weighted\_F1 (0,9782) sur la tâche multiclasse
indique que les classes de pannes rares sont sous-représentées. Deux leviers sont
identifiés : un suréchantillonnage \textbf{SMOTE} (génération d'exemples synthétiques
pour les classes minoritaires par interpolation) et un ajustement des poids de classe
à l'entraînement multiclasse. Enfin, l'ajout d'un réentraînement périodique piloté par
une détection de dérive (\textit{drift detection}) permettrait au modèle de s'adapter
à l'évolution du comportement des machines en production.

% ===================== SYNTHÈSE ============================
\subsection{Synthèse --- compétences démontrées}

Le \texttt{projet data science} couvre l'intégralité du périmètre du Bloc 4,
de la donnée brute à la mise en service, en démontrant chacune des cinq
sous-compétences du référentiel.

\subsubsection{Preuves d'acquisition des compétences}
\label{sec:preuves}

Le tableau suivant récapitule, pour chacune des cinq sous-compétences du Bloc 4, la
réalisation concrète qui la démontre dans le projet et le résultat mesurable obtenu.

\needspace{14\baselineskip}
\begin{center}
    \begin{tabularx}{\linewidth}{|>{\raggedright\arraybackslash}p{4.2cm}|>{\raggedright\arraybackslash}X|}
        \hline
        \rowcolor{tableHeader}
        \thead{Compétence visée}                                   & \thead{Réalisation et preuve dans le projet} \\
        \hline
        Extraire des données de systèmes variés                    &
        Chargement et exploration d'un jeu de données de capteurs industriels
        (\texttt{data.py}, 6 variables, 2 cibles) avec séparation features/cible contrôlée.                       \\
        \hline
        Préparer et nettoyer les données                           &
        Pipeline \texttt{ColumnTransformer} : imputation médiane + standardisation
        (numériques), imputation modale + encodage one-hot (catégoriel), split
        stratifié 80/20.                                                                                          \\
        \hline
        Communication infographique (tableaux de bord interactifs) &
        Tableau de bord \textbf{Streamlit} à trois pages : exploration (EDA),
        comparaison des modèles, prédiction en temps réel via l'API.                                              \\
        \hline
        Développer un modèle prédictif supervisé                   &
        Deux tâches (classification binaire et multiclasse à 5 classes), quatre
        modèles candidats par tâche, sélection automatisée du meilleur.                                           \\
        \hline
        Évaluer et comparer les performances                       &
        Benchmark sur F1, précision, rappel, ROC-AUC, PR-AUC ; meilleur F1
        = \textbf{0,9775} (binaire) et weighted\_F1 = \textbf{0,9782} (multiclasse).                              \\
        \hline
    \end{tabularx}
\end{center}

\newpage
\section{Bloc 5 -- Concevoir une strategie de management et de gouvernance de données pour transformer les données en informations creatrices de valeur}

Cette partie doit montrer votre capacite a relier les enjeux techniques aux
enjeux organisationnels, reglementaires, qualitatifs et strategiques de la data.

\subsection{Competences visees}

\begin{itemize}
    \item Definir une gouvernance de données en mettant en place les politiques et
          les standards afin d'etablir les roles, les responsabilites et la propriete des
          données.
    \item Mettre en place les bonnes pratiques de gestion de données en appliquant
          les regulations en vigueur pour proteger les données et respecter la vie privee
          dans une logique de transparence.
    \item S'assurer que la qualite de la donnee permet d'atteindre les objectifs
          business en faisant un audit de qualite et en proposant des plans de prevention
          et de remediation en cas de non-qualite.
    \item Identifier l'approche strategique pour faire de la donnee un actif central
          ancre dans la culture de l'entreprise et integrer cette approche dans un plan de
          communication diffuse aux personnes concernees.
    \item Elaborer une strategie de securite afin de proteger les données en
          utilisant les technologies adequates et en mettant en place une politique
          d'acces aux données.
\end{itemize}

\subsection{Projets / TP associes}

\begin{itemize}
    \item Data Management \& gouvernance
\end{itemize}

\subsection{Analyse attendue}

Mettez en avant les enjeux de gouvernance, de qualite, de securite, de
conformite et de communication autour de la donnee, en montrant comment vos
travaux transforment la data en valeur pour l'organisation.

\newpage
\section{Conclusion}

Concluez en rappelant le fil conducteur du rapport, la progression observee sur
les deux annees de formation, les competences acquises et votre capacite a
argumenter, justifier vos choix et prendre du recul sur vos realisations.

Vous pouvez terminer cette conclusion par une ouverture vers la soutenance,
qui devra s'appuyer sur un support synthetique et pertinent et mettre en valeur
votre e-portfolio de competences.

\begin{thebibliography}{9}

\bibitem{rncp}
France Compétences.
\textit{Fiche RNCP36739 --- Expert en ingénierie de données}.
Disponible sur : \url{https://www.francecompetences.fr/recherche/rncp/36739/}
[consulté le 15 juin 2026].

\end{thebibliography}

\end{document}
